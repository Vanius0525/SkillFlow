# journal — 每次实跑的原始输出和当时的判断

一次跑一个文件，命名 `YYYY-MM-DD-<跑了什么>.md`。

## 为什么和 HANDOFF 分开

`HANDOFF-whitebox.md` 记的是**结论现在是什么**，会被后来的跑覆盖重写。这里记的是
**那一次到底打印了什么**，写完不再改。两者的区别在下面这种时候才显出来：

- §12.3c 判断层 27 是退化的，靠的是 `mismatched` 那一列的 `+0.799`。那个数字不在
  任何一份 summary 的结论里，它只出现在当时的终端输出中。
- §12.3g 判断 E6 多半是抽取坏了而不是 H5，靠的是 near 和 **far 两个口味给出逐字节
  相同的分布**。任何一次单独的跑都看不出这一点。

**结论会被修正，而修正需要原始数字还在。** 一个只保留最新结论的文档回答不了"上次
那个判断是基于什么"，而那恰恰是发现自己错了的唯一途径。

## 一个文件里放什么

1. **命令和 run id** —— 别人要重现的入口
2. **原样贴的终端输出** —— 不要摘要、不要重排，包括看不懂的告警
3. **当时的判断，以及它依赖哪个数字** —— 把"我认为 X"和"因为打印了 Y"分开写
4. **当时不确定的事** —— 这一条最容易漏，也最有价值：它是下一次该查什么的清单

## 索引

| 文件 | run id | 跑了什么 | 主要结论 |
|---|---|---|---|
| [2026-08-22-tierA-tierB.md](2026-08-22-tierA-tierB.md) | `20260822-031238` / `20260822-033149` | Tier A 整条梯队（1.7B）；Tier B e0 两对 + e7 | 基线低于随机**不是**格式问题；skill 修检索错、造选择错；Tier B 贴地板未过门槛；`report.py` 认不出 4/9 阶段 |
| [2026-08-24-tierB-v2.md](2026-08-24-tierB-v2.md) | `20260822-195002` | Tier B v2 选装置：e0 两对 + errors 两份 + e7 | v1 死在地板、v2 死在天花板那一端（基线 0.819，余量 18.1pp）；常数那份差 0.008 没过、方法那份是干净的零；双重分离**不成立**；e7 报两份互斥 skill 方向余弦 0.97，但**没有中性对照** |
| [2026-08-25-controls.md](2026-08-25-controls.md) | `20260825-112500` + 三次单跑 | e7 带中性对照；离域负对照；Tier A errors 重跑；E6 定性 | **中性对照打掉了 0.97**（filler 与 skill 余弦 0.945/0.955，两 skill 之间 0.972，filler 的 ‖d‖ 还最大）；离域负对照合格但 mode 对不上；「修好的 19 题 100% 来自检索错」是错的，**84% 来自 echo，主效应是 engagement**；E6 swing +3.43 确认 H1、`nan` 是抽取坏了；模型确认为 8B |
| [2026-08-25-tierA-full.md](2026-08-25-tierA-full.md) | `20260825-123259` | Tier A 整条梯队 12 个阶段 + e7-tierB | **e2 超额恢复：real +1.07、mean +1.58、mismatched +0.23** —— 补丁比真放文档还有效,而「无内容」的平均向量最好；errors 给出机制（文档自己制造错误,wrong_family 2→14）；E1 的 net 只占行为效应 3.1%,**不是**持续依赖；num 模式 0.000→0.000,Tier A 的灵敏度证明打折；**跑的是旧代码,四处新对照都没生效** |
| [2026-08-30-tierA-refit-tierB-margins.md](2026-08-30-tierA-refit-tierB-margins.md) | `20260830-163838` / `20260830-170507` | 换题集后的 Tier A 整跑；Tier B 轴间距 + 空白组 + DiD 首跑 | **§12.3m 两条预注册命中**（基线 0.085→0.231，Δacc +36.2→+20.5pp）；**但 Tier A 现在过不了自己的门槛**（CI [0.0,41.0]），恢复率没有分母；**filler 在 Tier A 上不是惰性的** —— Δacc −7.7pp 却把金 token 推得比 skill 还远（+2.194 CI 不含 0 vs +0.641 含 0），可解析率掉到 85%；**间距对文档在场并不免疫**（filler 三条轴 −2.3，CI 不含 0）**但 lean 免疫**（两轴差 0.007）；**DiD：只分离了一半**（const +1.449 不含 0，proc +0.723 含 0）；Tier A 修的是**选择**（85%）不是检索；E2 的 filler>skill 在新题集上更强 |
| [2026-09-03-lastlayer-bf16.md](2026-09-03-lastlayer-bf16.md) | `20260902-100854` + `patchcheck.py` | 把末层恒等剩下的 1.265 拆到底 | **不是 bug，是 bf16 的算术**：捕获跑长 N 的前向、打分跑长 N+1 的，因果注意力下两者数学相等而数值不等（fp32 下全部 0.0000，检查 7 每层 0.00%）；末层 11% 是因为 `max|h|` 从 776 掉到 288，噪声分母一缩就翻上来；**副产品更要紧**——bf16 的 gold logprob 在尾部题上带 0.5~2.5 nats 误差（gold 是 argmax 的题 ≤0.01），波及所有 logprob 类结论且**配对 bootstrap 不覆盖**；处置：梯队 a 改 fp32（`WB_DTYPE`），梯队 b 先量再定；**E7 Tier A 数字变了**，filler 峰值层 6→27，「filler 峰在早层」撤回 |
| [2026-09-03-tierA-fp32.md](2026-09-03-tierA-fp32.md) | `20260903-074552` | Tier A 在 fp32 下整跑 | **末层恒等 = 1.000**，补丁通路第一次端到端自证通过；**fp32 只把 Δlogprob 从 +0.641 挪到 +0.693**（Δacc 完全不变 +20.5pp）—— 单题 0.5–2.5 nats 的 bf16 误差在 39 题上平均掉了，结论未变，变的是可证明性；**新 acc 曲线**前 17 层平在无文档基线、17 层起跳、之后稳在有 skill 基线，并解掉了「中层 logprob 负值」的歧义（是前向被打坏，不是信息未汇聚）；门槛仍不过（logprob CI 下界 −2.289）；**E1 顺序拆分符号相反 = 测到位置不是内容**；E7 探针三条件全 1.00 含 no_skill，无意义 |
| [2026-09-03-e2-acc-verdict.md](2026-09-03-e2-acc-verdict.md) | `20260903-074552`（重读，非新跑） | 把 e2 采到却没打出来的准确率对照打出来，对 §12.3r (b) 的预注册判据 | **命中分支一**：filler 10/39 题贴着无文档基线 9/39，real 18/39 反超真放文档的 17/39 —— **准确率通道分得开内容和在场，logprob 通道分不开，两条都报**；**判据漏了一列**：别题向量在 acc 上恢复 75%(K=1)/87%(K=4)，而 logprob 上它是 −3.249、`report.py` 的判定读的正是那个比值所以一声没吭 —— 补丁带的是这份 skill 的**共享**状态，不到逐题粒度；mean 反过来（acc 贴地板、logprob +5.435 最高 = 离流形，抬概率没抬成 argmax）；**所有差值都是个位数题目，配对检验没跑**（`e2_acc.py` 已写好，本机断网） |
| [2026-09-04-e2-acc-real.md](2026-09-04-e2-acc-real.md) | `20260903-074552`（重读） | `e2_acc.py` 第一次跑真数据：两条通道的逐层曲线 + 配对检验 + 分组 | **层 17–20 是前向被打坏的区间，acc 的 argmax 峰值(19)落在里面**，对照在那里一起虚高；**层 21（预先定死的 best_layer）上 real 同时分得开 filler(p=0.064/0.031) 和别题向量(p=0.049)**，别题向量 0.205 低于无文档基线 —— §12.3s 的「恢复 75%/87%、不到逐题粒度」**撤回**；**skill 修好的 13 题：filler 每一层 0.000，层 21 real 12/13、别题 4/13**；聚合 logprob 没用是**组间抵消**（fixed +5.33 / broken −11.12，加权 −0.80 = 层 19 的 −1.15）；`broken` 组 filler 0.800 > real 0.400 = 向量把 skill 的**损害**也复现了；K=4≈K=1，「一个位置装不下」排掉 |
| [2026-09-04-tierb-patchcheck.md](2026-09-04-tierb-patchcheck.md) | `./tierb.sh --check` | 量 8B 的 bf16 误差，定 Tier B 的 dtype | **bf16 误差比 1.7B 小一个数量级**（最坏 0.0889 nats，均值 0.0111，对比 Tier A 的 0.5–2.5）—— 但**原因是这 8 题里 7 题 gold 就是 argmax**（`lp_yes` 全 0），是天花板问题的又一次现身，**这个数是下界，不适用于补丁把 gold 推进尾部之后**；bf16 下末层相对误差 **11.51%**、6d [BAD]，**fp32 下每层 0.00%、6d [OK]** —— 自证仍然只在 fp32 成立；**fp32 加载成功没 OOM**，但 patchcheck 的足迹远小于 e2 层扫描，装不装得下要看 `--smoke --dtype float32` |
