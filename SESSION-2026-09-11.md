# 2026-09-10/11 这一轮做了什么（会话总结）

一句话：**把 E11 的减法扩成一个完整的分解实验，加了三份对照文档和两种作答形式，
结果是主张变强了、边界变清楚了，并且原来挂着的那条 R vs F 读数被判掉了。**

产物索引：

| 文件 | 内容 |
|---|---|
| [`paper/skillvector.tex`](paper/skillvector.tex) | **论文**（ICLR 版式，`paper/build.sh` 编译，当前 0 error / 0 overfull / 15 页含附录）。**在 git 里** |
| [`HOWSKILLWORK/PAPER-SKILLVECTOR.md`](HOWSKILLWORK/PAPER-SKILLVECTOR.md) | **实验脉络**：主张 → 判据 → 证据对照表、章节安排、还缺什么、相关工作调研 |
| [`HANDOFF-whitebox.md`](HANDOFF-whitebox.md) §12.3ab | 本轮全部读数与代码变更，按主题分 21 小节 |
| [`IDEAS.md`](IDEAS.md) | 三条够独立成线的 idea（多选形式的测量学、单位置 patching 的作用域、读文档的通用位移） |

---

## 1. 你问的三件事

### (a) GATE-W2：那个 R vs F 分离**不算数**

中性臂跑完（300/300）。`wb_analyze --compare` 的判决：

```
layers where the separation is content-specific: NONE
```

层 10–16 的分离在 gold 上是 −0.0037 ~ −0.0070，在**错配 skill** 上是 −0.0035 ~ −0.0056 ——
**几乎逐点复现**。层 35 同理（−0.0093 vs −0.0105）。
**你的判断是对的：那是「有份文档在场」，不是内容。**

第二个独立证据同向：`wb_diffvec` 的分格几何显示 R 与 F 的 `‖d‖`、`‖g‖`、`cos(d,g)`
在层 30 上分别是 189.4/213.1/−0.469 与 185.4/205.2/−0.468 —— 三位数上相同。
**在最后一个 prompt 位置上，几何不区分「被救回来」和「没救回来」。**

### (b) 为什么 mean / filler 的 logprob 比 real 高

**按四格拆开，加法就闭合了**（层 21，n=39）：

| 格 | n | mean − real (nats) | 对聚合差 +2.747 的贡献 | acc real | acc mean |
|---|---|---|---|---|---|
| **R** 被救回 | 13 | −2.506 | −0.835（−30%） | **0.923** | 0.231 |
| **F** 没救回 | 17 | +5.907 | **+2.575（94%）** | 0.059 | 0.294 |
| K 本来就会 | 4 | −0.740 | −0.076 | 1.000 | 0.500 |
| **B** 被弄坏 | 5 | +8.455 | **+1.084（40%）** | 0.000 | 0.200 |

**F + B 贡献 133%。** 聚合读数把无内容的对照排前面，是因为题集里最大的一块
是「真 skill 本来就把 gold 的 logprob 压下去」的题，而只会把分布压平的干预做不到这件事
（`mean` 把四选项的熵从 0.187 抬到 0.972，上限 1.386）。
**先纠正一处记忆偏差：比 real 高的是 `mean` 和 `filler`，`mismatched` 是全场最差的。**

真 skill 上还有**第二种**失效方式：CoT 模式下 R 格是 0.05 / 0.05 / **0.95**（无/错配/gold），
而**裸答案 lp** 是 −16.27 / **−13.21** / −14.65 —— 错配 skill 排第一。

### (c) 不改写题目，按自由作答做 acc + logprob

做了，而且**它改变了整个研究的构型**：

| 模型 | 形式 | 无文档 | filler | gold skill |
|---|---|---|---|---|
| Qwen3-1.7B | 多选（n=200） | 0.180 | 0.145 | 0.300 |
| Qwen3-1.7B | **自由作答** | **0.000** | — | **0.000** |
| **Qwen3-8B** | **自由作答（n=120）** | **0.000** | **0.000** | **0.467** |

1.7B 在自由作答下拿到 skill 输出的是**换算表里的因子**（`21`/`180`/`60`），不做那步乘法。
**8B + 自由作答是全研究最干净的正对照**：地板 0、存在对照 0、效应 +46.7pp，四格塌成 R=56/F=64。

---

## 2. 本轮最强的四条新结果

### ⚠ 2.1 多选形式下，一张**因子全错**的表值 67% 的效应；自由作答下值 0%

| 上下文里的文档 | 多选（1.7B） | 占效应比例 | **自由作答（8B）** | **占比** |
|---|---|---|---|---|
| 无文档 | 0.180 | — | 0.000 | — |
| 同一份文档、**因子全换** | 0.260 | **0.67** | **0.000** | **0.00** |
| 同一份文档、**行顺序打乱** | 0.270 | 0.75 | 0.325 | 0.70 |
| gold | 0.300 | 1.00 | 0.467 | 1.00 |

预注册命中。**多选考的是「有没有一份长得像表的文档」，自由作答才考表本身。**

### ⚠ 2.2 单位置补丁搬运的是**答案**，不是内容 —— 而且这是形式不是规模

**同一个 8B、同一批题、同一个干预**：多选下整体替换修回 R 格的 **1.000**，
自由作答下 **0.304**。这 120 道题的 gold 答案 token 数是 2/3/4，**单 token 占 0.0%**。
容量曲线：k=1 → 0.286，k=4 → 0.286，**k=16 → 0.554**。

### 2.3 内容成分的二级分解：共同偏移占 0.9 的模长、0.05 的效果

把 `d_i` 沿共享方向 `u` 拆成平行与正交两半，单独注入（R 格 n=48）：

| 注入 | L18 | L20 | **L21** | L24 | L26 |
|---|---|---|---|---|---|
| `d` 整块 | 0.396 | 0.438 | 0.479 | 0.708 | **0.812** |
| **共同偏移** `(d·u)u` | 0.354 | 0.292 | 0.125 | 0.042 | 0.062 |
| **逐题残差** `d^⊥` | 0.083 | 0.146 | **0.521** | **0.583** | **0.604** |

**层 21 上两半互换角色。** 层 16–20（打坏区）只有共享方向起作用 ——
这是对「那一段的 recovery 是通用扰动」的独立确认。

### 2.4 补丁忠实复现文档的**全部**行为，包括它的失败和破坏

层 26，四格：`replace_real` 给出 **R 1.000 / F 0.000 / K 1.000 / B 0.000** ——
逐格等于文档自己按定义该有的样子。8B 上是 1.000/0.024/1.000/0.000。
`add_g` 与「真的把那份对照放进 prompt」在**四份不同的对照文档**上吻合到 0.02 以内。

---

## 3. 必须自己说出来的分寸

1. **分解在构造上可加，在效果上不可加**：k=16 层 26 上整体 0.518，
   而 `d` 单独 0.196、`g` 单独 0.000。k=1 时 `d` 能拿到整体的 88%，k=16 时只有 38%。
   **「内容成分就是全部」这句话不能说。** 站得住的是：**`g` 单独注入在所有 k、
   所有层、所有 α 上都是惰性的。**
2. **「内容成分被低估、放大 α 就补回来」只在 k=1 上成立**（多选 α=2 → 0.979）；
   k=16 下最优点回到 α=1。
3. **`cos(d,g)` 不是独立读数**（余弦定理，实测吻合 0.002）。
   非平凡的那一半是 **层 0–16 上 `‖g‖ > ‖t‖`：无关文档把状态推得比真 skill 还远**。
4. **真 skill 上的因果读数目前功效不足**（n=20，单位置补丁 0.05–0.15）。
5. **`wb_patch` 的 `ANSWER:` cue 会把 MedCalc 上的效应完全抹平**（三臂全是 0.175），
   所以真 skill 的因果实验一律走 CoT 模式，`--max-new 900`（400 会截断）。
6. 修掉两处判据/代码错误：GATE-W0 的 argmax 门槛（0.99 → 0.95，是长度门槛不是正确性门槛）、
   `wb_diffvec` 的 lp 补丁位置用了相对下标 `-1`（落在答案末尾，什么也没改；解码通道不受影响）。

---

## 3b. 第二轮（回答「为什么 1100 题上不 work」）

| # | 结果 | 数字 |
|---|---|---|
| 3b.1 | **答案长度是受控原因** | 同 80 题同模型：1 token → **1.000**，2–4 token → 0.304，思维链 → **0.000**（skill 本身 0→1.000） |
| 3b.2 | **`d` 能认出 skill，但搬不动** | 42 类留一 ncm **0.93–1.00**（chance 0.024）；注入同一个 `d` 修回 ≈0 |
| 3b.3 | **在场成分低于无文档基线** | ncm(g) 0.30–0.38 vs ncm(h₀) 0.43–0.53（须用 `--fixed-filler`） |
| 3b.4 | **形状** | skill 之间 ≈6 维，skill 内 ≈32–38 维（层 22 后塌到 12–24） |
| 3b.5 | **span 的零是我的接收方错了** | 不等长 → RoPE 差 ~900 位，`real`=`ctrl`=0.200；等长后题目 span 仍收敛到零，**文档自己那段 span 层 10 上 1.000（n=2）** |

**两个被对照抓住的自身错误**：中性文档一一配对（`g` 假装知道 skill）、`participation_ratio` 没去均值。

## 4. 论文当前状态

`paper/skillvector.tex`，标题 *Presence or Content? Decomposing What a Skill Document
Injects into a Language Model*。0 error / 0 overfull / 15 页（正文 ~11 + 附录）。
**正文超出 ICLR 的 9 页，需要再压一轮**——这是已知的、唯一的结构性待办。
`\PEND{...}` 宏会把未填的数字标成红色，`build.sh` 会数出来。
`skillvector.bib` 里有 8 条作者字段是 `Anonymous`（检索只拿到 arXiv id），**投稿前必须补**。
