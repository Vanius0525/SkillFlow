# 战役文档：位置预算实验 + 正文全量补跑（2026-09-18 起）

周期检查（cron `17 */4 * * *`，会话内）每次先读本文件 §5 的清单。
**本文件是这一轮实验的唯一调度记录**；结论写进 §6，结束后并入 HANDOFF。

---

## 0. 目标与优先级

| 优先级 | 内容 | 为什么 |
|---|---|---|
| **P0** | **位置预算曲线**（新实验，§1） | 现稿从「最后一个位置」直接跳到「整段 skill span（415–1,630 位置）」，恢复率上升究竟来自 skill 的位置结构，还是只是注入了更多状态——这是当前最关键的混杂 |
| P1 | Figure 2 补全：Mistral 曲线、0.6B 修复后全量曲线、bottom-k 全网格、8B 补 k 点 | 见 §2.1：Mistral 数据早就有但没画；bottom-k 只跑过 16/64；0.6B 曲线用的是修复前的 `lad06-fine` |
| P1 | Table 1 修复并全量重跑 | 见 §2.2：pre-fix 的原因是 Tier A 代码路径里 capture 不带 attention mask、decode 带 |
| P1 | 全层注入 / normrecv / dall（HANDOFF §3 的待办） | 论证支点仍是 pre-fix 单个数 |
| P1 | 窗口实验用统一接收方重跑 | 见 §2.3：发现窗口臂的接收方与电池不同 |
| P2 | 其余正文非全量：Fig 1 顶栏、题干 span 移植、Table 3 空格（0.6B/Mistral 全量行）、§4.1 分解、PR 几何 | 用户要求「正文里所有还不是全量的实验都补跑」 |

---

## 1. 位置预算实验（P0）——预注册，**在任何数据回来之前写定**（2026-09-18）

### 1.1 设计

- 模型/任务/层：Qwen3-8B × MedCalc，层 8（电池同层），doc-first，fixedskill 匹配接收方。
- 样本：`full-battery-a` 的受限集，**135 题 / 14 calculator**（`--calcs 13,18,23,24,29,32,33,36,43,49,60,64,67,7`）。
  与表 2、秩曲线、深度扫描是**同一批题**。基线（receiver / gold）按 instance_id 从 `full-battery-a` 合并。
  所有臂只读本题自身的 d，不涉及跨题供体池，所以按 calculator 过滤是安全的（模块里断言）。
- 预算 B ∈ {1, 4, 16, 64, 256, ⌊m/2⌋, m}（m = span 宽，415–1,630）。
- 每个 B 上只改变「选哪 B 个位置」：

| 臂 | 选位 | 写入内容 | 回答的问题 |
|---|---|---|---|
| `ev` 保序 | 均匀等距 | 各位置自己的 h_gold | 覆盖均匀、顺序保持时要多少位置 |
| `rn` 随机选位 | 均匀随机 | 各位置自己的 h_gold | 无结构子样本 |
| `mx` **错位** | 与 `rn` **同一组状态**，循环平移 m/2 后写入 | h_recv(P') + d(P) | 注入向量、数量、能量完全相同，只有落点错 |
| `td` 最大 ‖d‖ | ‖d_p‖ 最大的 B 个 | 自己的 h_gold | 「注入更多状态」的最强版本 |
| `wf` 语义区域：流程 | 以 `### Computation`/`### Scoring Criteria` 为中心的连续 B 窗 | 自己的 h_gold | 同预算下内容位置是否重要 |
| `wd` 语义区域：描述 | 以标题+描述段为中心的连续 B 窗 | 同上 | 语义贫乏区域对照 |
| `we` 语义区域：示例 | 以 `### Example` 为中心的连续 B 窗 | 同上 | |
| `wr` 随机连续窗 | 随机中心的连续 B 窗 | 同上 | 把「连续性」与「语义」分开 |
| `tq` span 外 | 最后 B 个 prompt 位置（题干尾+指令+模板） | 有 skill 那次 forward 的状态 | 同预算下写在 skill 之外；`tq1` 即经典最后位置补丁，但与其余臂同接收方 |
| `self` / `real` | 全 span | h_recv / h_gold | 仪器检查：必须逐题复现电池的 receiver / real_L8 |

随机性按 (instance, 策略, B) 哈希播种，与分片、处理顺序无关；`mx` 永远平移 `rn` 选中的那组位置。
每个臂额外记录：实际位置数、写入的内容能量占比 Σ‖d_p‖²/Σ‖d‖²、落在 D/F/E 三个区段的比例。

区段在 span 中的占比（dry-run，calc 64 / 13）：D 9–11%、F 34–39%、E 29–30%。
**已知局限**：D 只有 56–64 个 token，所以 `wd64`/`wd256` 必然溢出到相邻区段；分析时按实际落区比例报告，不把 `wd256` 当成「纯描述」。
`tq256` 在短 prompt 上被截到 span 之后的可用长度，记录实际数。

### 1.2 两个竞争假设及其可证伪预测

- **H-state（注入量）**：恢复率只由 B（或注入能量）决定。
  预测：同 B 下 `mx ≈ rn`、`tq ≈ rn`、`td ≥ rn`；ρ 对能量占比的回归解释大部分方差，策略项不显著。
- **H-struct（位置结构）**：需要 skill 的状态在 skill 自己的位置，并且是对的位置。
  预测：同 B 下 `mx ≪ rn`（至少在 `rn` 已显著 >0 的 B 上）；`tq` 在所有 B 上 ≈ 0；`wf > wd`；控制能量后策略项仍显著。

我对结果的**事先预期**（写下来是为了事后不能挪靶）：
1. `ev/rn` 在 B ≤ 64 时 ≈ 0，恢复只在 B ≥ ⌊m/2⌋ 附近出现，且呈阈值形（类比剂量曲线在 α 0.55→0.7 的台阶）。
2. `mx` 在所有 B 上 ≈ 0（`mxfull` 预计 ≤ 0.10，与随机置换 0.05 同档）。
3. `tq` 在所有 B 上 ≤ 0.05。
4. `wf256` > `wd256`，且 `wf256` 高于同预算的 `rn256`（与四分实验中公式段 0.56 一致）。
5. `td` 不领先 `rn`。

**判据**：预测 2、3 同时成立 → 支持 H-struct，正文可写「不是注入量」。
若 `mx` 在某个 B 上与 `rn` 的配对差 CI 覆盖 0 且 `rn` 显著 >0 → 该 B 上 H-state 未被排除，如实写。

### 1.3 分析方案（事先固定）

- 主读数：每臂 ρ = (acc − acc_recv)/(acc_gold − acc_recv)，receiver/gold 取自 `full-battery-a`（受限集上 receiver = 0）。
- 区间：按 calculator 的 cluster bootstrap（14 组，B=10,000）**和** item bootstrap 都报；组少，以 cluster 为主。
- 配对对比（同题）：`rn` vs `mx`、`rn` vs `tq`、`wf` vs `wd`、`td` vs `rn`，每个 B 上 McNemar 精确检验 + 配对差的 cluster bootstrap CI。
  多重比较：每个对比族 5–7 个 B，Holm 校正。
- 回归：逐题逐臂的 logistic GLMM 近似（calculator 聚类稳健 SE）：`ok ~ log2(B) + energy_share + strategy`。
- 仪器检查：`self` 必须逐题等于电池 receiver（135/135），`real` 必须逐题等于 `full-battery-a` 的 `real_L8`。**任一不等 → 全部分片作废重跑。**

### 1.4 相关工作核对（2026-09-18，开跑后第一时间检索）

| 工作 | 出处 | 一句话 | 与本实验的关系 |
|---|---|---|---|
| Label Words are Anchors | Wang et al., EMNLP 2023, arXiv 2305.14160 | ICL 中示例信息在浅层汇聚到少数 label word 位置，深层从那里读出 | 预测「少数对的位置就够」；我们的 `td`/区域臂在长推理答案、程序性 skill 上检验这个 anchor 图景是否成立 |
| Probing the Prompt KV Cache: Where It Becomes Dispensable | Bannihatti Kumar et al.（AWS），arXiv 2605.30574，2026-05 | 替换 L 层以上的 prompt KV：上层只需「形式」（空白脚手架即可恢复 ~88% GSM8K），下层内容被真正读取 | 与我们「交接早于读取结束」的深度分离同方向；**他们只做全位置、不变位置子集** → 位置预算正是其空白 |
| KV 压缩在推理上的评估（H2O / SnapKV 等；"Hold Onto That Thought", OpenReview） | 2024–2025 | 按注意力选 heavy-hitter token 保留，推理任务上掉点 | 是「读取侧保留哪些 token」，我们是「注入侧写入哪些 token」；`td`（最大‖d‖）是写入侧的 heavy-hitter 对照 |
| Function / task vectors | Todd et al. ICLR 2024；Hendel et al. EMNLP 2023 | 单位置向量可搬运 ICL 任务 | 位置预算 B=1 端；正文已讨论其答案格式依赖 |

结论：**没有找到在同一预算下分离「对位 / 错位 / 语义区域 / 能量」的工作**，该实验的 delta 成立。

---

## 2. 开工前发现的问题（都要修）

### 2.1 Figure 2（`fig-rank-medcalc`）

1. **Mistral 不是没跑完，是没接线**：`mis3-rank-L4`（141 行，k=4/16/32/64/128 + bottom16/64）早就完成，
   `figs.py::fig_rank` 有 `family=` 分支，但 `main()` 调用时从未传 `family=`。图注却写了 Mistral k*=84 → **图注与图不符**。
2. **bottom-k 只有两个点**：历来只跑了 `rank16lo`、`rank64lo` 两个臂（全量 `full-rank-b` 也是）。
3. **0.6B 曲线是修复前数据**：`figs.py` 取 `lad06-fine`（无 dataset 字段、fixmask 之前），只有 k=24–64；
   表 3 用的却是修复后的 `fixmask-lad06-rank`（k=4/16/32/64/128）。图注写「after the instrument fix」→ **不符**。
4. 三条曲线都不是全量（0.6B 234/467 题，Mistral 141/221 题），误差棒是 item bootstrap。

**补法**：三个模型都按全量（8B/0.6B 467 题，Mistral 221 题 / 39 calc）跑统一 k 网格
{1,2,4,8,16,24,32,48,64,96,128,256} + bottom-k {4,16,32,64,128}；8B 只补缺的 k（受限 14 calc）。

### 2.2 Table 1 为什么是 pre-fix

`whitebox/model.py::capture_block_outputs` 调 `r.model(ids, use_cache=False)` **不传 attention_mask**，
而 `model.generate` 传了全 1 mask → sdpa 走两条不同 kernel（is_causal 快路径 vs 显式 mask），
这正是 MedCalc 那条线 6.1% null 臂翻转的同一个 bug。影响：
- 数值题（0.304）与 CoT（0.000）两行：捕获无 mask、解码有 mask → **受影响**。
- 多选行（1.000）：读数是 `score_with_patch` 的选项 argmax，两边都无 mask → 内部一致，但仍标 pre-fix。

**另外两个 reviewer 能挑的问题**：
- 三行的层网格不一样：多选 8 层（12–35）、数值 16 层（0–35）、**CoT 只测了 30/32/35 三层**，却都报「best over tested layers」。
- 样本：多选/数值 120 题、CoT 80 题，而 Tier A 共 358 题。
- 没有 identity（self）臂。

**补法**：`capture_block_outputs`、`score_with_patch`、`first_token` 统一传显式 mask；e14 加 `replace_self` 臂；
三种形式全部 358 题、同一层网格；CoT 按层分片。

### 2.3 ⚠ 窗口实验与电池的接收方不同（新发现，正文图 3c 受影响）

`wb_spanpatch --matched` 把 span 覆盖成**整个 control prompt 的前 m 个 token**（chat 模板+system+配对中性文档），
而电池/深度/秩用 `wb_spanvec --filler fixedskill`（一篇固定的错误临床 skill）。
实测：两种接收方在 467 题上 **只有 358 题行为一致**；受限集上窗口接收方 0.015 vs 电池 0.000；
同是 L16 单层注入，窗口 run 0.096 vs 深度 run 0.052。
而 `main_evidence.py` 用 `rep.load(['full-window','full-battery-a'])` 合并，**电池的基线覆盖了窗口 run 自己的基线** →
窗口臂是在 A 接收方上测的，却用 B 接收方的基线归一化。TheoremQA 的 `tqa-window` 同理。

**补法**：`wb_spanpatch` 新增 `--filler fixedskill`（复用 `wb_spanvec.filler_ids_for`，并同样剔除以 filler 为 gold 的题）。
已在远端逐题核对：新接收方与电池 **467/467 题 token 与 span 完全一致**。窗口 4 个 + L16 + 全层（0:35）全部重跑。

### 2.4 论文里的陈旧数字（改稿时一并修）

- 摘要：`0.87 … forty-calculator evaluation`、`0.13 at rank 32` → 全量是 0.89、0.12。
- 引言：`recovers 0.87`、`32 leading directions recover 0.13`、`knee from 52 to 40` → 0.89 / 0.12 / 47。
- §4.6：`Qwen3-8B's 47 [43,72]` → 全量区间是 [43,55]（[43,72] 是子集的）。
- §4.3：开发集句子（n=16 的 0.00→1.00、0.19 vs 0.94、剂量开发集）旁边已有全量数，改为只引全量或降为 provenance。

### 2.5 TheoremQA 行不是严格全量

旧作业用 `--per-calc 4 --max-calcs 400`，每个 skill 组最多取 4 题：8B 168/170、0.6B 168/172、Mistral 142/143。差 1–4 题，优先级最低。

---

## 3. 作业表

8B 解码实测约 15 s/次（全量均值）。「题」= 实际解码的题数。

### 3.1 已派发

| host | tag | 模块 | 内容 | 题 | 解码/题 | 估时 |
|---|---|---|---|---|---|---|
| hsw | x8-extra | wb_spanvec | dall, normrecv（doc-first L8） | 467 | 2 | ~4 h |
| hsw2 | x8-win-a | wb_spanpatch fixedskill | L16 单层 + self + 窗口 0:35（全层）+ 8:11 | 467 | 4 | ~8 h |
| hsw3 | x8-dl-dall → x8-win-b | spanvec / spanpatch | doc-last dall；窗口 12:15、14:19、16:35 | 467 | 1 / 3 | 2 h + 6 h |
| wb | mis-bat-a | wb_spanvec | Mistral 全量电池 A（+基线）L4 | 221 | 8 | ~6 h |
| wb2 ×2 进程 | pb-ev / pb-we | wb_posbudget | self, real, ev×6 / we×5 | 135 | 8 / 5 | 双进程测吞吐 |
| wb3 | pb-rn | wb_posbudget | rn×6 | 135 | 6 | ~3.5 h |
| wb4 | pb-mx | wb_posbudget | mx×7 | 135 | 7 | ~4 h |
| sra1 | pb-wf | wb_posbudget | wf×5 | 135 | 5 | ~3 h |
| sra2 | pb-wd | wb_posbudget | wd×5 | 135 | 5 | ~3 h |
| sra3 | pb-td | wb_posbudget | td×5 | 135 | 5 | ~3 h |
| sra4 ×2 进程 | pb-tq / pb-wr | wb_posbudget | tq×5 / wr×5 | 135 | 5 / 5 | 双进程 |

### 3.2 已入队（runner 在当前作业结束后自动接续；队列文件 `whitebox/queues/<host>[.2].txt`）

| host / 队列 | 顺序 |
|---|---|
| hsw | x8-extra → **x8-rank-x**（8B 受限：k=1,2,8,24,48,96,256）→ **x8-rank-lo**（bottom 4,32,128）→ geom-8b → geom-06 → geom-17 |
| hsw2 | x8-win-a → **fp-c**（最后位置/题干：tq1,tq16,tqall @ L4,8,24） |
| hsw3 | x8-dl-dall → x8-win-b → **t8-win**（TheoremQA 窗口，fixedskill） |
| wb | mis-bat-a → mis-bat-b → mis-rank-a（k=1–24）→ mis-rank-b（k=32–256）→ mis-depth-a（L0–3,5–8） |
| wb2 / jobs | pb-ev → q06-bat-a → q06-rank-a → q06-depth-a → q06-ko → t06-ko |
| wb2 / jobs2 | pb-we → q06-bat-b → q06-rank-b → q06-rank-lo → q06-depth-b → t06-bat |
| wb3 | pb-rn → **tA-mc** → tA-num-a（偶数层）→ tA-num-b（奇数层） |
| wb4 | pb-mx → tA-num-k4 → tA-num-k16 |
| sra1 | pb-wf → **fp-a**（tq1,tq16,tqall @ L10,14）→ tA-cot-1 |
| sra2 | pb-wd → **fp-b**（@ L20,30）→ tA-cot-2 |
| sra3 | pb-td → tA-cot-3 → d17-neutral → d17-shuffled → d17-corrupted |
| sra4 / jobs | pb-tq → tA-cot-4 → tA-cot-self |
| sra4 / jobs2 | pb-wr → mis-rank-lo → mis-depth-b → mis-ko |

说明：
- **Figure 1 顶栏改设计**：旧 `wb_diffvec` 无 `--resume`、结果全攒在内存最后才写、接收方是旧的配对中性文档（与 span 栏不同）。
  改用 `wb_posbudget` 的 `tq1`（最后位置）/`tq16`/`tqall`（span 之后全部位置 = 题干 span 移植的匹配接收方版本），
  在 L4,8,10,14,20,24,30 上跑，与 span 栏**同 135 题、同接收方**，同时覆盖正文「question-span transplantation … seven tested layers」。
  旧的无 skill 接收方分解（+g presence）留在合成任务 §4.1，不再在 MedCalc 上用小样本撑。
- Table 1：三种形式都是 358 题、36 层全网格（CoT 按层 4 片 + identity 片），`replace_self` 必须逐题 = 基线。
- §4.1：1.7B fp32，358 题，原 run 的层表与臂保持不变。
- 冒烟（hsw3，修复后）：数值题生成读数 identity 8/8 精确；多选读数改为在与捕获同长度的 prompt 前向上取 argmax 后 identity 24/24 精确、末层自检 10/10。

### 3.3 吞吐实测（09-18 01:40）

8B 单进程 11–14 s/解码；同卡双进程合计 1.1–1.4×；Mistral ~6 s/解码。

---

## 4. 运维

- 远端：`whitebox/qrun.sh [name]` 常驻，读 `/root/q/<name>.txt`，一个作业接一个；完成写 `/root/out/<tag>.done`（镜像到共享盘）；
  队列空写 `/root/q/<name>.idle`。重启后从共享盘镜像恢复 jsonl 和 done 标记，`--resume` 续跑。
- 本地：`whitebox/qstart.sh <host> [--push]` 下发 `whitebox/queues/<host>.txt`（和 `.2.txt`）并确保 runner 在。
- 结果：`whitebox/mirror.sh` 3 分钟镜像到 `ssd/.../wbout/<容器名>/`；本地 `autofetch.sh` 拉回 `fetched/by-host/<host>/`。

---

## 5. 周期检查清单（每 4 小时）

1. `inspire notebook list --workspace 可上网GPU资源`：有 STOPPED 且队列未完成的 → start → connection refresh（常要两次）→ `ssh-keygen -R "[wt-gpu-<h>]:22222"` → `./whitebox/qstart.sh <h> --push`。
2. 每台 RUNNING：`ssh inspire-me-wt-gpu-<h> 'tail -3 /root/q/qrun.log; nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader; ls /root/q/*.idle 2>/dev/null'`。
   **GPU 利用率 <15% 或有 .idle** → 从 §3.2 派新作业；没有可派的 → `inspire notebook stop`。
3. 看每个运行中作业 log 尾部有无 Traceback / `[WARN] … rows, expected`；`.failed` 标记。
4. 本地守护进程在不在（看 ppid），`logs/fleet.log` 尾部。
5. 已完成作业核对行数（位置预算分片应 135 行）并跑仪器检查（§1.3 最后一条）。
6. 在 §7 追加一条检查记录。

---

## 6. 结论（数据回来后填）

### 6.0 过程中的观察（实时记录，未到结论）

- 09-18 02:10，位置预算分片各 9–27 题（1–2 个 calculator）：**写 256 个分散位置（ev/rn/td）≈ 0，
  而同样 256 个连续位置落在流程段 `wf256` = 1.00（27 题）、随机连续窗 `wr256` 0.53、描述段 `wd256` 0.50**；
  能量占比几乎一样（wf256 0.35 vs rn256 0.37）。`evhalf`（一半位置、一半能量）仍 ≈ 0。
  若全量成立：恢复既不由位置数也不由注入能量决定，而是由**连续性 + 内容位置**决定。
  ⚠ 样本极小、calculator 极少，**不要引用**；等 135 题齐了按 §1.3 判定。
  事后要问的反事实：连续窗的优势是「相邻位置状态自洽」还是「窗口恰好覆盖了公式 token」？
  → `wr256`（随机位置的连续窗）已经给出部分答案；若 `wr` 也显著高于 `rn`，说明连续性本身就是因素。
- 同时：受限集 span 宽度是 **365–1,630**（正文 415–1,630 是 40-calc 子集的）。

### 6.1 已准备好的生成器（数据齐后直接跑）

| 产物 | 脚本 | 新数据来源 | 回退 |
|---|---|---|---|
| 新实验图表 + 对比表 | `whitebox/analysis/posbudget_report.py` → `paper/fig-posbudget.*`、`whitebox/analysis/out/posbudget.{json,md}` | pb-*、fp-* | — |
| Figure 2 | `paper/fig_rank_full.py` | full-rank-*、x8-rank-*、q06-*、mis-* | 首个新 tag 不存在时用旧 tag |
| Figure 1 | `paper/fig_channels_full.py` | full-battery-a/b + pb-tq + fp-a/b/c | — |
| Table 1 | `paper/main_evidence.py::answer_format_full` | `whitebox/results/fetched/tA/<tag>/`（需手动从盒子/镜像拉回） | 36 层不齐则保留旧表 |
| Figure 3c 窗口 | `paper/main_evidence.py::depth` | x8-win-a/b（467 行齐才切换） | full-window |
| Table 3 | `paper/replication.py`（`NEW` + `complete()`） | q06-*、mis-*、t06-* | 各 tag 行数不够则用旧 tag |

---

## 7. 检查日志

- 09-18 01:10 前后：11 台全部 RUNNING；首批 4 个补跑作业 + 9 个位置预算分片已派发。
- 09-18 01:45：本地 `fleet.sh`（setsid，600 s）与 `autofetch.sh` 常驻；首轮 11 台 GPU 72–100%。
- 09-18 02:00：53 个作业全部入队；位置预算前 9 题仪器检查 self 9/9、real 9/9 精确。
  Tier A 修复冒烟：数值题 identity 8/8、多选 identity 24/24、末层自检 10/10。
- 09-18 04:47（第 1 次周期检查）：11 台全 RUNNING，GPU 67–100%，无 .failed、无 Traceback、无 fleet.alert。
  **已完成**：pb-rn / pb-wf / pb-wd / pb-td（各 135 行）、x8-dl-dall（467）、mis-bat-a（221）。
  进行中：pb-ev 71、pb-we 112、pb-mx 122、pb-tq 96、pb-wr 98、x8-extra 374/467、x8-win-a 278/467、
  x8-win-b 39、fp-a 25、fp-b 19、mis-bat-b 37、tA-mc、tA-cot-3。
  仪器检查（71 题）：self 71/71、real 71/71 精确。
  **处理动作**：读了首批完整分片的结果后加了两组新臂（见 §1.5），插到 wb4 / sra4-jobs2 / sra1 / sra2 队列的第 2 位（P0 优先于 P2）。

## 8. 首批完整分片的结果（135 题，4 个臂族齐全；2026-09-18 04:50）

| 预算 | 分散取位 `ev` | 随机取位 `rn` | 最大‖d‖ `td` | 错位 `mx` | span 外 `tq` | 连续窗（流程）`wf` | 连续窗（示例）`we` | 连续窗（随机）`wr` |
|---|---|---|---|---|---|---|---|---|
| 1 | 0.00 | 0.02 | 0.07 | 0.03 | 0.01 | 0.02 | 0.02 | 0.00 |
| 4 | 0.00 | 0.03 | 0.08 | 0.03 | 0.01 | 0.03 | 0.01 | 0.01 |
| 16 | 0.00 | 0.04 | 0.06 | 0.01 | 0.02 | 0.02 | 0.04 | 0.02 |
| 64 | 0.01 | 0.04 | 0.09 | 0.03 | 0.03 | 0.06 | 0.02 | 0.02 |
| 256 | 0.01 | **0.07** | 0.12 | 0.05 | 0.01 | **0.40** | 0.08 | **0.37** |
| 半个 span | 0.06 | 0.10 | — | 0.03 | — | （已派 `pb-wh`） | | |
| 整个 span | **0.89** | 0.89 | 0.89 | **0.03** | — | 0.89 | 0.89 | 0.89 |

（`mx` = 与 `rn` 完全相同的一组状态、整体平移半个 span；`tq` 最大 234 个位置；四分位对照：Q1 .09 / **Q2 .56** / Q3 .12 / Q4 .05；整段随机置换 `dshuf` .05）

**三条已经稳的结论**（cluster bootstrap，n=135 / 14 calc）：
1. **不是注入量**：256 个分散位置（能量占比 0.29–0.45）≤ 0.12，而同样 256 个**连续**位置 0.37–0.40；
   一半位置（能量 0.5）分散只有 0.06–0.10。`td`（专挑能量最大的位置）也只有 0.12。
2. **位置必须对**：整段内容平移半个 span 后 0.03 vs 原位 0.89，配对差 **+0.85 [0.69, 0.95]**，McNemar p=6e-30。
3. **写在 skill 之外没用**：最后 1/4/16/64/234 个 prompt 位置全部 ≤ 0.03（`tq1` 就是经典的最后位置补丁，
   这里与 span 臂同接收方同题）。

**新出现的问题（正是加新臂的原因）**：
- 同预算 256 下，连续 ≫ 分散，但**落在哪一段的差别没那么大**（流程 .40 / 随机窗 .37 / 描述窗 .33），
  只有**示例段显著更差**（`we256` vs `wr256` 配对差 **−0.30 [−0.44,−0.12]**，Holm 后 p=4e-6）。
  原因之一是预算 256 时窗口必然溢出区段（描述段只有 56–68 token），要按实际落区比例分析。
- 于是新增 §1.5 的两组臂，把「连续性」与「内容位置」彻底分开。

## 1.5 追加的两组臂（2026-09-18 04:50，基于首批结果，预注册式记录）

- `cNxB`：**预算固定**（256 或半个 span）、注入能量固定，只改变这些位置被切成**几个连续块**
  （N=1 一整块 → N=32 高度碎片化）。预测：ρ 随 N 单调下降；若 N=1 ≫ N=32，则「连续性」本身就是变量。
- `shD`：**整段 span 的内容**整体平移 D ∈ {1,4,16,64,256} 个位置。预测：若平移 1 个位置就掉到 0，
  说明要求的是精确的绝对位置（RoPE 相位）；若平移 16 仍可，则要求的是局部邻域一致性。
  这条同时给「错位」效应一个连续的量纲。
- `pb-wh`：连续窗在**半个 span** 预算下的四个区段版本，补上 256 → 整段之间的空档。

- 09-18 20:34（第 2 次检查；08:17/12:17/16:17 三次**没有触发**）：
  **事故**：本机 WSL 在 ~07:00 宕机/重启（`uptime` 26 min），本地 `fleet.sh`/`autofetch.sh` 随之消失，会话内 cron 也没触发；
  重启后 WSL 的 DNS 代理一度走了有线网卡的公共 DNS，`qz.sii.edu.cn` NXDOMAIN（Windows 经 WLAN DNS 10.11.26.11 解析为 10.252.252.3），
  20:50 前后自行恢复。
  **平台侧**：10/11 台在北京时间 12:20–20:40 **先后**被停（`Stop reason` 显示为账号用户），只剩 sra4。
  重启后检查：这 10 台的队列**早已全部跑完**（空闲 288–782 分钟），所以是「队列空 → GPU 0% → 被平台停」，
  而本地守护进程恰好不在、没能先停掉它们。**结论：平台确实会停掉低利用率实例，fleet.sh 的空闲停机是必要的。**
  处理：10 台重启 → refresh → push → qstart（/root/out 在重启后仍保留）；重启 autofetch 与 fleet；
  fleet 随即把 10 台队列已空的机器正确停掉。**只剩 sra4 的 `mis-ko`（165/221）在跑。**
  另：按用户指示先把仓库与当前论文推到 GitHub（`e5c76c3..445db75`，4 个 commit），**此后不改 paper/ 下任何文件**（另一个 agent 在改论文）。
- 09-18 22:40：全部结果经 sra4 从共享盘镜像拉回并校验（逐行可解析、行数=唯一题数=预期）。
  **发现**：今晚重启后的容器 `/root/out` 只保留文件名、内容为空或残缺（平台停机快照不一致）；
  jsonl 按行数取最大副本不受影响，e14 目录改按总字节取副本。`d17-neutral` 第 24–26 层在镜像里就是空的
  （mirror 复制撞上写入），已在 sra4 补跑 `d17-neutral-b`（L24–27）。
  新数字全部写在 `whitebox/analysis/out/{posbudget,campaign-numbers}.{json,md}` 与 `fig-posbudget.*`，**不写 paper/**。
  最后一组追加臂（边界对照，见 §9.4）在 wb/wb2 上跑，约 2.3 h；`mis-ko` 183/221。

## 9. 结果（全量，2026-09-18 22:40）

### 9.1 位置预算（135 题 / 14 calc，仪器检查 self 135/135、real 135/135、跨作业重复臂 135/135 ×2）

| 臂 | 1 | 4 | 16 | 64 | 256 | 半段 | 整段 |
|---|---|---|---|---|---|---|---|
| 均匀（保序） | .02 | .03 | .06 | .04 | .13 | .15 | .89 |
| 随机 | .02 | .03 | .04 | .04 | .07 | .10 | .89 |
| 最大‖d‖ | .07 | .08 | .06 | .09 | .12 | — | .89 |
| 错位（同状态平移半段） | .03 | .03 | .01 | .03 | .06 | .03 | .06 |
| span 外最后 B 位 | .03 | .02 | .06 | .05 | .05 | — | — |
| 连续窗·流程段 | .02 | .03 | .02 | .06 | .40 | **.82** | .89 |
| 连续窗·描述段 | .04 | .05 | .06 | .13 | .33 | .80 | .89 |
| 连续窗·随机 | .03 | .04 | .02 | .05 | .33 | .49 | .89 |
| 连续窗·示例段 | .02 | .04 | .03 | .04 | .10 | .18 | .89 |

- 分块（预算、能量固定）：256 位 1/2/4/8/16/32 块 = .37/.24/.22/.13/.08/.08；半段 1/2/4/8/16 块 = .55/.41/.40/.22/.28。
  1 块 vs 32 块（256）配对差 +0.29 [0.09, 0.48]，McNemar 42:6，p=1e-7。
- 整段平移 D：0 → .89，1 → **.18**，4 → .06，16 → .03，64 → .07，256 → .06，半段 → .06；随机置换 .05。
- 最后位置 / 最后 16 位 / span 后全部位置，在层 4/8/10/14/20/24/30：全部 ≤ .07。
- 连续块按覆盖流程段比例分四档：256 预算 .17/.20/.36/.49；半段预算 .31/.52/.73/.71。

**预注册判定**（§1.2）：①B≤64 时 ev/rn≈0 ✓，但「半段附近出现阈值」✗——分散取半段仍 ≤.15，只有整段才恢复；
②错位全 B ≈0（mxfull .06 ≤ .10）✓；③span 外 ≤.05：基本成立，个别格 .06–.07，**如实写 ≤.07**；
④wf256>wd256 ✗（.40 vs .33，CI 重叠；256 窗口溢出区段）、wf256>rn256 ✓（.40 vs .07）；⑤td 不领先 rn ✓（Holm 后不显著）。
→ 按判据支持 H-struct：**恢复不由注入的位置数或能量决定，而由连续覆盖、落在流程段、且逐位精确放置决定**。
§1.5 的两条追加预测：分块单调下降 ✓（半段 16 块略高于 8 块，在 CI 内）；平移 1 位即崩 ✓（.18）。

### 9.2 正文已有实验的全量新数（详见 `whitebox/analysis/out/campaign-numbers.md`）

| 位置 | 旧（子集 / pre-fix） | 新（全量、修复后） | 结论是否要改措辞 |
|---|---|---|---|
| §4.4 dall（doc-first / doc-last） | .11 / .06 pre-fix | **.03** [0,.08] / **.04** | 否，更强 |
| §4.4 normrecv vs real | 1.06 vs 1.06 pre-fix | **.87 vs .89** | 否 |
| §4.6 全层注入 | .950 pre-fix | **.98** [.91,1.03]，identity 467/467 | 否，去掉 pre-fix |
| 图 3c 窗口 8–11/12–15/14–19/16–35/L16 | .91/.74/.10/.10/.10 | **.89/.74/.16/.06/.06**（与电池同接收方） | 「14 起与 16 起一样无用」→ .16 vs .06，仍都很低，措辞可保留但数要换 |
| TheoremQA 窗口 | .90/.73/.45/.21（=L16 .21） | **.84/.61/.38/.20（=L16 .20）** | 否 |
| 图 2 Qwen3-8B | k=4…128 五点，bottom 2 点 | 14 个 k（k=256 达 .89），bottom-k 5 点全 ≤.04；k* 47 [43,55] | 否 |
| 图 2 / 表 3 Qwen3-0.6B | 204 题（35 calc），k* 40 [32,50] | **362 题（38 calc），ρ .91 [.75,1.05]，k* 43 [39,52]**；bottom ≤.03；handover .36–.39，reading .86 | 宽度等比预测 12 仍被拒绝 |
| 图 2 / 表 3 Mistral-7B | 92 题（26 calc），ρ .92，k* 84 [59,97] | **92 题（20 calc），ρ .84 [.69,1.09]，k* 72 [46,94]**；bottom-k .11–.21 | ⚠ **「k* 随模型家族不同」变弱**：72 [46,94] vs 47 [43,55] 区间重叠 |
| 表 3 最大对照 | ≤ .27 | 0.6B 的 dcross **.30**，Mistral dfar .28 | 「≤0.27」要改为「≤0.30」 |
| 表 1 多选/数值/CoT | 1.000 / .304 / .000（120/120/80 题，层网格不同） | **1.000 / .235 / .003**（358 题，统一 36 层，identity 12888/12888、12888/12888、1074/1074） | 否，数要换 |
| §4.2 数值题 k=1/4/16 | .286/.286/.554 | **.235/.261/.403** | 否 |
| §4.2 数值题 content / presence | .268 / .018 | **.21 / .008** | 否 |
| §4.1 shuffled / corrupted 负对照 | .19–.29 / .23–.31 | **.17–.26 / .17–.26** | 否 |
| §4.7 多选在数字被破坏后仍保留 | ~3/4 | presence **.72–.74** | 否 |
| §4.6 massive activation | 40 份中 34 份，99–180× | **48 个 calc 中 35 个（96 题中 70 题），121–195×，全在维度 676/2276**；0.6B/1.7B 无 | 数要换 |
| span 宽度 | 415–1,630 | **365–1,630**（受限 135 题） | 数要换 |


- 09-19 01:31（周期检查）：10/11 RUNNING，GPU 61–100%，无 .failed。sra4 被平台停（已连续运行约 24 h，疑为时长上限）→ 手动重启、从断点续跑。
  **已完成**：mis-ko（221）、d17-neutral-b（第 26、27 层完整；24、25 层在镜像里又是 0 字节——mirror 的 `cp -ru` 竞争，**已修**：按大小比对 + 临时名原子替换，已推到全部机器并重启镜像）。
  §4.1 全量修复后：presence .098（= wrong-skill 基线 .098）、content .805、四格 .805/.075/1.000/.186，identity 9308/9308。
  **第一批已并入正文**（本地 commit 00602aa）：表 1、图 2（三模型全量、画到 k=256）、表 3 全量行、窗口改用电池接收方 + 全层 .98、谱诊断、span 宽度、删除开发集句子、秩拐点家族差异收窄（两个任务上区间都重叠）。
  **追加两个便宜的读数诊断全量重跑**（§4.7 仍是修复前小样本）：rd-e2（1.7B fp32，358 题，hsw）、rd-cue（8B，R+F 全部，hsw2）。
  补层作业进度：ko1-mc8-* 87–114/134，dep1-mc8-a/b 38/135（与 Mistral 深度同卡，较慢），pb-sn 90、pb-sx 103、win1-a 79、win1-b 97（/135）。

## 10. 正文「未覆盖全部层 / 未用全量」的盘点与补齐（2026-09-19 用户要求）

| 正文位置 | 问题 | 补法（作业） | 状态 |
|---|---|---|---|
| 图 3(a) 单层 span 注入 | 横轴 0–35，只测了 0,4,8,12–20 | dep1-mc8-a..d（补 24 层，受限 135 题） | 跑 |
| 图 3(b) 注意力敲除 | 步长 4；且与 (a) 不是同一批题 | ko1-mc8-a..f（补 27 层，受限∩R 134 题，与 (a) 配对） | 跑 |
| 图 3(c) 层窗口 | 类别条形、顺序混乱（16, 8–11, 12–15…）、只覆盖部分层、接收方与 (a) 不同 | x8-win-a/b（统一接收方）+ win1-a/b（0–3…32–35 平铺） | 前者完成、后者跑 |
| 图 1 上栏 最后位置 | 只有第 8 层 | fp-a/b/c（7 层）+ fp1-a..d（其余 29 层） | 跑 |
| 表 3 handover / reading | 各行深度、敲除网格粗（步长 4 或不规则） | dep1-* 与 ko1-*（6 个模型×任务行，受限题上补到逐层） | 跑 |
| §4.7 读数诊断（3.75 nats / +0.60 nat） | 39 / 40 题小样本（前者 pre-fix） | rd-e2（358 题）、rd-cue（R+F 全部） | 跑 |
| TheoremQA 三行 | 每组最多 4 题（168/170 等，差 1–4 题） | 未补：换抽样会改变所有 TQA 行的题集，收益 1–2% | 向用户说明 |
| 表 4 下半（问题依赖） | 只在第 8、16 层两个点 | 设计上的两点对比，不是曲线 | 向用户说明 |

**图 3 横坐标的问题与新设计**：旧图 (a) 横轴到 35 但数据止于 20、间隔不均；(b) 横轴是「从第 L 层起一直屏蔽到最后」，
右移=屏蔽更少，与 (a) 的方向含义相反，且步长 4；(c) 是无序、互相重叠的类别条（单层 16 与窗口混排）。
新版 `main_evidence.depth_v2`：两栏共用「层 0–35」横轴、每层都测：(a) 三条曲线（整段写在第 L 层 / 从第 L 层起屏蔽注意力 / 只写最后位置），
标出两条边界与其间「仍被读取但已无法转移」的区间，两条主曲线在同一批题上配对；(b) 每个窗口画成横跨其层范围的横条、高度=恢复率。
数据齐全前自动保留旧图。

### 9.3 位置预算：边界对照（2026-09-19 02:10，135 题，完成）

| 臂 | ρ [cluster 95%] |
|---|---|
| 整段原位 real | .89 [.76,.97] |
| 循环平移 1 / 4 / 16 / 64 / 256 | .18 / .06 / .03 / .07 / .06 |
| 不卷绕前移 1 / 4 / 16 | .15 [.09,.24] / .05 / .06 |
| 不卷绕后移 1 / 4 / 16 | .22 [.12,.38] / .12 / .07 |
| 原位去掉前 1 / 16 位 | .89 / .90 |
| 原位去掉后 1 / 16 位 | .89 / .92 |

→ 平移 1 位即失效**不是**卷绕或边界造成的；要求的是逐位精确放置。记入 IDEAS.md I6（值得单独立项）。
新图 `whitebox/analysis/out/fig-posbudget.pdf` 四栏定稿；尚未放进论文，等用户决定位置（建议替换图 1，见汇报）。

