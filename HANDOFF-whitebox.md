# HANDOFF — 白盒实验：skill 注入在模型内部到底做了什么

研究问题：**为什么把 skill 放进上下文能提高任务表现？模型内部发生了什么变化？**

对象模型：Qwen3-8B（和 agent harness 那边同一份权重，`models/Qwen3-8B`）。

这份文档随实验推进实时更新。`HANDOFF.md` 是 agent harness（黑盒对比实验）的，
两者共用模型和 skill 库，但实验方法完全不同。

最后更新：2026-08-19 · 状态：**设计阶段，尚未开跑**

---

## 0. 开跑之前必须先解决的三件事

### 0.1 vLLM 做不了白盒

当前配置是 vLLM 起 OpenAI 兼容端点，只能拿到 token 和 logprob。白盒需要中间激活
——注意力权重、residual stream、各层输出——这些必须用 HF transformers 直接加载
权重、挂 forward hook 才拿得到。

**这不是给 vLLM 加个参数就行的事，是另一条推理路径。** 两边并存即可（vLLM 继续
服务 agent harness 那批黑盒实验，白盒这边单独起 HF 进程），但显存要错开：一台
4090 装不下两个 8B。

显存账（bf16）：

| 项 | 估算 |
|---|---|
| Qwen3-8B 权重 | ~16 GB |
| KV cache（4k 上下文，batch=1） | ~1 GB |
| 激活缓存（全层全位置 residual） | ~1.2 GB / 次前向 |
| 24 GB 卡余量 | 紧，但够 batch=1 |

结论：**batch=1，只缓存选定的层和位置**，不要无脑存全部。真要全存就落盘。

### 0.2 模型结构参数从 config.json 读，不要凭记忆写

层数、hidden size、head 数、GQA 的 KV head 数，全部从
`models/Qwen3-8B/config.json` 读。所有实验脚本里的层索引都应该由代码算出来，不
要硬编码。

```bash
python -c "import json;c=json.load(open('models/Qwen3-8B/config.json'));print({k:c[k] for k in c if 'layer' in k or 'head' in k or 'hidden' in k})"
```

### 0.3 thinking mode 必须先定死

Qwen3 有 thinking / non-thinking 两种模式，思维链长度不定。白盒实验里这是致命的
混杂因素：CoT 一长，"skill 的作用"就和"多想了几百个 token"混在一起分不开。

**默认关闭 thinking**，把它作为**独立的一个实验条件**放到后期，而不是让它污染主
实验。关闭方式和 chat template 有关，实测确认，不要假设。

---

## 1. 把"为什么有帮助"拆成可证伪的假设

"skill 有用"不是一个机制，至少是四个。它们**预测不同的内部信号**，所以可以分开
检验——这是整个设计能成立的前提。

| 假设 | 机制 | 内部信号预测 | 行为预测 |
|---|---|---|---|
| **H1 检索/复制** | 模型从 skill 文本里搬运字面内容（API 名、公式、常数），靠注意力回指 | 生成时对 skill span 有持续、集中的注意力；中后层；敲除该注意力→崩 | skill 里的具体符号出现在输出里 |
| **H2 任务识别** | skill 只是**选中**了权重里已有的能力，知识本来就有 | 效果集中在早/中层的 residual 偏移；可压成单个向量 | 换成语义等价但措辞不同的 skill 也一样有效 |
| **H3 格式约束** | 减少的是格式错误而非推理错误 | 效果集中在末层/logit | 错误类型分布变化，而非难度分布 |
| **H4 长程锚点** | 提供可反复回看的计划，减少长生成漂移 | 注意力回指随生成变长而增强 | 效果随答案长度增长 |

四者不互斥，真实情况多半是混合。**目标不是选出唯一赢家，而是给出各自的份额，并
且指出哪类 skill 走哪条路。**

> 最有价值的单一结论会是：**H1 和 H2 的分界线在哪里**。如果多数 skill 走 H2，那
> "skill 有用"本质上是 prompt 找对了开关，而不是提供了信息——这对怎么写 skill、
> 要不要压缩 skill、多 skill 怎么选，全是直接的指导。

---

## 2. Phase 0：先确认存在值得解释的效应

**这一步不做完不要碰任何内部指标。** 如果 skill 只带来几个点的提升，那点差异会
被采样噪声吃掉，后面所有机制分析都是在解释噪声。

### 任务集要求

- 答案短且可判定（数值 / 单个符号 / 固定选项），不要开放式
- 单轮，不用工具，不用多步 agent 循环
- 有天花板也有地板：无 skill 时准确率不能接近 0 或接近 1，否则没有可解释的差值
- 每个条件至少 100 题，否则区分不出 5 个点的差异

**GAIA 不适合**：多步、用工具、生成长，内部信号会被 agent 循环稀释。
**SciBench 更合适**：短数值答案、有现成的 `scibench_skills/`、仓库里已有 scorer。

### 通过门槛

```
无 skill 准确率 ∈ [0.2, 0.7]
有 skill 提升   ≥ 15 个百分点，且 bootstrap 95% CI 下界 > 5 个百分点
```

**达不到就换任务或换 skill，不要硬着头皮往下做。** 这个门槛是整个项目的第一个
决策点，宁可在这里多花两周。

### 产出

一张 `(task_set, skill) → Δaccuracy` 表，按效应量排序。后面所有白盒实验只在效应
最大的 2–3 对上做，不要全面铺开。

---

## 3. 核心实验

### E1 注意力敲除：skill 在哪些层被读取

对每一层（或每几层一组），把**所有位置指向 skill span 的注意力**屏蔽掉，测准确率
下降。得到一条"层 → 依赖度"曲线。

- 掉得最多的层 = skill 信息实际被读入的地方
- 如果集中在早层 → 偏 H2；分散在中后层且持续 → 偏 H1

**陷阱**：直接置零会把注意力分布推到分布外，掉分可能来自"模型懵了"而不是"信息没
了"。对照组必须做：屏蔽**同等长度的无关文本 span**，看掉多少。真实效应 = 屏蔽
skill 的掉分 − 屏蔽等长无关文本的掉分。

### E2 激活补丁 / task vector：**H1 与 H2 的判决实验**

这是整套设计里信息量最大的一个。

1. 跑「有 skill」，缓存第 ℓ 层在提示词最后一个位置的 residual stream
2. 跑「无 skill」，在第 ℓ 层把该位置的 residual **替换**成上面缓存的向量
3. 看准确率恢复了多少

- **恢复大部分** → skill 的作用可以压缩成一个向量 → 模型不需要持续访问 skill 文
  本 → **支持 H2**（选中已有能力）
- **几乎不恢复** → 模型需要一直回看 skill 里的字面内容 → **支持 H1**（检索）

对 ℓ 做层扫描，得到"最佳注入层"。方法源头是 ICL task vector / function vector
那一系（Hendel et al.、Todd et al.），但那边是 few-shot 示例，这里是文档形式的
skill，**能不能压成向量本身就是一个开放问题**——这正是值得做的地方。

**分层预测**：偏事实型的 skill（API 签名、具体常数）应该压不动；偏流程型的 skill
（"先做 X 再做 Y"）应该压得动。如果这个分裂真的出现，就是一个干净的结论。

### E3 logit lens：正确答案在第几层浮现

用 unembedding 把每层的 residual 投影到词表，看正确答案 token 的概率随层数的上升
曲线，对比有无 skill。

- 曲线整体左移（更早浮现）→ skill 加速了已有计算 → H2
- 只在最后几层拉开 → H3（格式/输出层面）

用 tuned lens 比 raw logit lens 更可靠，但要额外训练探针，先用 raw 看趋势。

### E4 skill 分块消融：**"每个部分怎么 work"**

把 SKILL.md 按结构切开，逐块删除后重测。仓库里的 skill 有稳定结构：

| 块 | 内容 |
|---|---|
| frontmatter `name` | 名字 |
| frontmatter `description` | 何时使用、触发条件 |
| 标题 + 概述 | 这个 skill 是干什么的 |
| 步骤/流程 | 怎么做 |
| 表格/参数 | 具体取值 |
| 代码块 | 可复制的片段 |
| 示例 | 输入输出样例 |

每块做两件事：删掉测行为，同时看 E1/E2 的内部指标怎么变。

**这里才是能直接回答用户问题的地方**：如果删掉「description」主要影响 E2 的
task vector（模型不知道该切换到什么模式），而删掉「代码块」主要影响 E1 的注意力
回指（没东西可抄了），那 H1/H2 就和 skill 的具体部位对应上了。

### E5 单 skill vs 多 skill

固定「相关 skill 一定在」，变化无关 skill 的数量 k。

**三个必须分开的效应**：

1. **长度成本**：上下文变长本身就掉分
2. **语义干扰**：无关 skill 抢注意力
3. **选择失败**：模型选错了 skill

**对照设计**（缺了就分不开）：

| 条件 | 作用 |
|---|---|
| 相关 skill 单独 | 基线 |
| 相关 + k 个无关 skill | 干扰 + 长度 |
| 相关 + 等长的随机文本 | **只有长度** |
| 相关 + 等长的乱序 skill 文本 | 长度 + 表面统计，无语义 |
| 只有 k 个无关 skill | 选择失败的下界 |

内部指标：
- **注意力份额**：指向相关 skill 的注意力占全部 skill span 的比例，随 k 怎么衰减
- **是否存在"选择层"**：训练一个线性探针，从第 ℓ 层 residual 预测「哪个 skill 是
  相关的」。探针准确率随层数的曲线如果有明显拐点，那一层就是模型做选择的地方。
  这个结果本身就有独立价值。

---

## 4. 影响注入效果的因素

E1–E5 之外要扫的自变量。每一个都可能是混杂因素，所以主实验里要固定，单独扫时其
他全固定。

| 因素 | 取值 | 为什么值得看 |
|---|---|---|
| 位置 | system 开头 / 结尾 / 紧贴问题前 | RoPE 下相对距离影响注意力 |
| 长度 | 原文 / 压缩版 / 摘要 | 仓库里 `condenser.py` 已经在做压缩，正好可以验证压缩掉的是不是有用信息 |
| 特异性 | 精确匹配 / 同领域泛化 / 无关 | 区分 H1（要精确）和 H2（泛化也行） |
| 数量 k | 1 / 3 / 8 | 见 E5 |
| 格式 | markdown 结构 / 平铺散文 / 打乱段落 | 结构本身是否携带信息 |
| **任务是否真的需要** | 模型本来就会 / 本来不会 | **最重要的调节变量**，见下 |

最后一条单独说：如果模型本来就会做这个任务，skill 的提升只能来自 H2/H3（选择或
格式）；如果模型本来不会，提升必然含 H1（提供了新信息）。**按这个轴把任务分成两
组，是分离假设最省力的办法**——比任何内部指标都直接。设计任务集时就要考虑进去。

---

## 5. 已知陷阱

写在前面，因为每一条都足以让整批结果作废。

1. **注意力权重不等于信息流。** 高注意力不代表那条边重要，必须用因果干预（敲除、
   补丁）而不是只看注意力图。只报告注意力可视化是没有说服力的。

2. **激活补丁的位置选择很敏感。** 补最后一个 token 还是补所有位置，补哪一层，结
   果差别很大。先用小规模扫一遍再定，不要一上来就跑全量。

3. **置零消融是分布外操作。** 优先用均值消融（替换成该位置在一批样本上的平均激
   活）或用无关文本替换，而不是直接置零。

4. **多重比较。** 层 × 头 × 条件很快就是几千次比较。先定好要检验的假设，或者用
   FDR 校正，不要跑完之后挑显著的讲故事。

5. **thinking mode 泄漏。** 见 §0.3。

6. **skill 里的答案泄漏。** 有些 skill 可能直接包含了测试题的答案或近似内容。跑
   之前必须查一遍 skill 文本和测试集的重叠，否则测的是记忆不是机制。

7. **单一模型的结论不能外推。** 全部结论只对 Qwen3-8B 成立。有余力时至少在
   Qwen3-4B 或 14B 上复现关键结论，看是否随规模变化。

---

## 6. 执行步骤

每步给出**做什么 / 具体要求 / 完成判据**。完成判据不满足就不要进下一步——这个
领域最常见的失败不是做错，是在地基没打好的时候往上堆分析。

### 全局纪律（每一步都适用）

| 项 | 要求 | 为什么 |
|---|---|---|
| 解码 | **greedy，temperature=0**，全程 | 有采样就分不清"skill 的作用"和"这次抽样运气好" |
| 配对 | 同一批题目跑所有条件，用 **McNemar** 而不是两独立比例检验 | 配对设计功效高得多，题目难度差异被消掉 |
| 冻结 | 任务集和 skill 文本存成文件并记 hash | skill 改一个字结论就可能变，必须知道跑的是哪一版 |
| run 目录 | 照搬 `experiments-agents.sh` 那套 `RUN_ID` + `run-info.txt` | 白盒条件组合多，更需要每次跑不覆盖上一次 |
| 层索引 | 由 `config.json` 算，不硬编码 | 见 §0.2 |

---

### 第 1 步：可挂 hook 的前向链路

**做什么**：用 HF transformers 加载 `models/Qwen3-8B`（bf16，`device_map` 单卡），
封装成一个函数：给定 prompt，返回 logits + 指定层在指定位置的 residual stream +
指定层的注意力权重。

**具体要求**：

1. 从 `config.json` 读结构参数，打印出来存档（层数 L、hidden、head 数、KV head 数）
2. **关闭 thinking mode**，并且验证：把渲染后的 prompt 打印出来，确认没有 think
   相关的特殊 token。不要假设参数名对，实测。
3. **HF 与 vLLM 的一致性校验**（这步最容易被跳过，跳过了后面全白做）：
   同一个 prompt，两边各取前若干 token 的 logits，比较。
   要求：**top-1 token 完全一致，top-5 的 logprob 差 < 0.05**。
   对不上就说明白盒观察到的模型和黑盒测出来的不是同一个计算，两边结论不能互相
   引用。常见原因是 dtype、chat template、rope scaling 配置不同。
4. hook 只在**需要的层和位置**上取，取完立刻 `.detach().cpu()`
5. 显存实测：跑一个 4k 上下文的前向，记录峰值占用

**完成判据**：
- 一致性校验通过
- 4k 上下文 batch=1 前向不 OOM，峰值有记录
- 能画出任意一层任意位置的 residual 向量

**坑**：vLLM 和 HF 抢显存，跑白盒前先 `./run-server.sh stop`。

---

### 第 2 步：任务集 + scorer

**做什么**：从 SciBench 挑一批题，配上 skill，做成冻结的数据文件。

**具体要求**：

1. 题目：短答案（数值/符号/选项）、单轮、不用工具
2. **每个候选 (任务集, skill) 对至少 100 题**。100 题足够检出 15 个点的差异；
   后面层扫描太贵时可以降到 50，但门槛判定必须用满 100
3. scorer 必须**先人工抽检 20 题**，确认判对判错和人的判断一致。
   scorer 脆弱是个隐形陷阱：如果它对格式挑剔，"skill 有帮助"可能只是"skill 让
   输出变得可解析"——那是 H3，但你会误以为测到了推理能力
4. **污染检查**：把每个 skill 的文本和测试题做重叠比对（n-gram 或字符串匹配）。
   skill 里直接含答案的题目全部剔除，否则测的是记忆不是机制
5. 存成一个文件，记 hash：题目 id、题干、标准答案、对应 skill 名

**完成判据**：
- 冻结的任务集文件 + hash
- scorer 人工抽检一致
- 污染检查报告（剔除了几题，为什么）

---

### 第 3 步：效应筛查（**第一个决策点**）

**做什么**：对每个候选 (任务集, skill) 对，测有 skill 和无 skill 的准确率。

**具体要求**：

1. 两个条件唯一的差别就是 system prompt 里有没有那段 skill 文本，其他全同
2. greedy 解码，配对
3. 算 Δ 准确率 + bootstrap 95% CI（配对 bootstrap，重采样题目）
4. 顺便记录：输出长度、是否触发格式错误

**完成判据（硬门槛）**：

```
无 skill 准确率 ∈ [0.2, 0.7]
Δ ≥ 15 个百分点
配对 bootstrap 95% CI 下界 > 5 个百分点
```

**三条全满足才进第 4 步。** 一条不满足就换任务或换 skill，不要硬做。没有效应就
没有可解释的对象，后面所有机制分析都是在解释噪声。

产出一张按效应量排序的表，**只取前 2–3 对**进入后续实验。不要全面铺开——层扫描
的成本是题数 × 层数，条件一多就跑不完。

---

### 第 4 步：错误类型学（便宜，但先做）

**做什么**：把第 3 步里"无 skill 做错、有 skill 做对"的题目，人工或用 LLM 分类。

**具体要求**：分成四类，每类给出比例

| 类别 | 含义 | 指向 |
|---|---|---|
| 格式错 | 方法对、算对，输出格式不合要求 | H3 |
| 方法错 | 选了错误的解法/公式 | H2 |
| 知识缺失 | 不知道某个常数/API/定义 | H1 |
| 计算错 | 方法对但算错 | 都不指向，属噪声 |

**完成判据**：一张比例表，至少 50 个题目样本。

**为什么放在机制实验之前**：这一步几乎不花算力，却能提前告诉你四个假设的大致
份额。如果 80% 是格式错，那 H3 就是主线，后面重心该放在 logit lens 而不是激活
补丁——**能省掉几周跑错方向的时间**。

---

### 第 5 步：E2 激活补丁（**判决实验**）

**做什么**：见 §3 E2。这是信息量最大的一步。

**具体要求**：

1. 构造配对 prompt：`with_skill` 和 `without_skill`，任务文本完全相同，只差 skill
   段落。**注意两者长度不同**，所以"最后一个 prompt token"的绝对位置不同——按语
   义位置（prompt 的最末 token）对齐，不是按绝对下标
2. 对每层 ℓ ∈ [0, L)：
   - 跑 `with_skill`，缓存第 ℓ 层最后一个 prompt token 的 residual
   - 跑 `without_skill`，在第 ℓ 层同一语义位置**替换**成缓存值，然后生成、判分
3. **实现上的坑**：带 KV cache 生成时，补丁必须挂在 **prefill 那一次前向**上，不
   是每个 decode step 都挂。挂错了等于在生成过程中反复注入，结果没有意义。
   实现完先验证：补丁层设成一个不该有效果的层，结果应该和 `without_skill` 一致
4. 两个变体都跑：只补最后一个 token / 补任务段落的所有位置
5. **必须有的对照**：
   - 补一个**无关任务的 skill 向量**（应该没有恢复效果）
   - 补该位置在一批样本上的**均值向量**（消融基线）
6. 指标：`恢复率 = (acc_patched − acc_no_skill) / (acc_skill − acc_no_skill)`

**完成判据**：一条恢复率 vs 层数的曲线，带上面两个对照。

**这一步的结果决定后面的重心**：

- 恢复率高（某层 > 0.5）→ 效果可压成向量 → **H2**。第 6 步的注意力敲除降级为验
  证，重心转向"这个向量编码了什么"（比如它和不同 skill 的向量能不能聚类）
- 恢复率低（全层 < 0.2）→ 模型需要持续读 skill 文本 → **H1**。第 6 步升级为主线

**成本估算**：L 层 × 50 题 × (1 次缓存前向 + 1 次补丁生成)。8B 在 4090 上单题约
数秒，全扫大概几小时量级——**这是估算，第 1 步测完实际速度再排期**。

---

### 第 6 步：E1 注意力敲除

**做什么**：屏蔽指向 skill span 的注意力，做层扫描。

**具体要求**：

1. 分组扫描（比如每 4 层一组）先定位，再在热点区域逐层细扫。不要一上来就逐层×
   逐头，那是几千次比较
2. **对照是必须的**：同样屏蔽一段**等长的无关文本** span。
   真实效应 = 屏蔽 skill 的掉分 − 屏蔽等长无关文本的掉分
3. 优先用均值消融或替换成无关文本，**不要直接置零**——置零是分布外操作，掉分可
   能来自"模型懵了"而不是"信息没了"
4. 找到热点层之后再做逐头分析，此时用 FDR 校正

**完成判据**：层 → 依赖度曲线（已扣除对照），热点层列表。

---

### 第 7 步：E3 logit lens

**做什么**：把每层 residual 投影到词表，看正确答案 token 概率的层间上升曲线。

**具体要求**：

1. 先用 raw logit lens 看趋势，够用就不上 tuned lens（后者要额外训探针）
2. 对比有无 skill 的两条曲线，看是**整体左移**（更早浮现，指向 H2）还是**只在末
   几层拉开**（指向 H3）
3. 只对答案是**单 token** 或首 token 就能判定的题做，多 token 答案的 lens 结果
   不好解释

**完成判据**：两条曲线叠图 + 一个"浮现层"的定义和数值。

---

### 第 8 步：E4 分块消融

**做什么**：按 §3 E4 的分块表，逐块删除后重测行为，并对每个变体重跑第 5/6 步的
关键指标（只跑热点层，不用全扫）。

**具体要求**：

1. 分块要**机械可复现**：用 markdown 结构解析，不要手工剪。写个 splitter，对每
   个 skill 输出结构化的块
2. 每块两种处理：**删除** 和 **替换成等长占位文本**（区分"少了信息"和"少了长度"）
3. 交叉看：哪一块的删除主要伤 E2 的恢复率，哪一块主要伤 E1 的注意力依赖

**完成判据**：块 × (行为 Δ, E2 恢复率变化, E1 依赖度变化) 的表。

**这一步直接回答"skill 每个部分怎么 work"**——如果删 description 主要伤 E2、删代
码块主要伤 E1，那 H1/H2 就和 skill 的具体部位对应上了。

---

### 第 9 步：E5 多 skill

**做什么**：见 §3 E5。

**具体要求**：

1. 五个条件全跑（§3 E5 的对照表），**缺一个就分不清干扰和长度**
2. k 至少取 1 / 3 / 8 三档
3. 无关 skill 的选取要固定随机种子，并且报告是哪几个——不同的无关 skill 干扰程度
   可能差很多
4. 探针实验：从第 ℓ 层 residual 训线性探针预测"哪个 skill 相关"。
   **必须留出独立测试集**，并报告随机基线（1/k）

**完成判据**：
- 准确率 vs k 的曲线，三条（无关 skill / 等长随机文本 / 等长乱序 skill）
- 注意力份额 vs k
- 探针准确率 vs 层，带随机基线

---

### 第 10 步：因素扫描

按 §4 的表逐项扫。**主实验的因素全部固定**，一次只动一个。

优先级：位置 > 数量（已在第 9 步）> 特异性 > 长度 > 格式。

**特别值得做的**：§4 最后一行那个「任务是否真的需要」的分组。把任务分成"模型本
来就会"和"本来不会"两组，分别看 skill 的效果——**这比任何内部指标都更直接地分离
H1 和 H2**，而且成本很低。

---

### 步骤依赖图

```
第1步 链路 ──┬─→ 第3步 效应筛查 ──→ 第4步 错误类型学
第2步 任务集 ─┘         │                    │
                        │   (决定重心)        │
                        ↓                    ↓
                    第5步 E2 激活补丁 ←───────┘
                        │
              ┌─────────┴─────────┐
         恢复率高                恢复率低
              ↓                    ↓
      "向量编码了什么"        第6步 E1 注意力(主线)
              │                    │
              └─────────┬──────────┘
                        ↓
              第7步 lens → 第8步 分块 → 第9步 多skill → 第10步 因素
```

第 5 步放在第 6 步前面是有意的：判决实验的结果决定往哪投入，先做判决再投入。

---

## 7. 代码组织（建议）

白盒代码和 agent harness 分开，避免互相污染：

```
whitebox/
    model.py          HF 加载 + hook 管理（唯一碰权重的地方）
    tasks/            任务集 + scorer
    skills/           实验用 skill（可以是 scibench_skills 的软链）
    e0_effect.py      P1 效应筛查
    e1_knockout.py    注意力敲除
    e2_patch.py       激活补丁
    e3_lens.py        logit lens
    e4_ablate.py      分块消融
    e5_multi.py       多 skill
    results/<run-id>/ 沿用 agent harness 那边的 run 目录约定
```

run 目录、`run-info.txt`、`RUN_ID` 续跑那套直接照搬 `experiments-agents.sh` 的做
法——白盒实验条件组合更多，更需要"每次跑不覆盖上一次"。

---

## 8. 待确认

- [ ] `models/Qwen3-8B/config.json` 的层数 / hidden / head 配置
- [ ] 4090 上 HF bf16 加载 + hook 的实际显存占用
- [ ] thinking mode 的关闭方式（chat template 层面）
- [ ] vLLM 和 HF 进程的显存调度（要不要停掉 vLLM）
- [ ] skill 文本与候选测试集的重叠检查
- [ ] 任务集选型：SciBench 子集是否满足 §2 的准确率区间
