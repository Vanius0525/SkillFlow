# IDEAS — 够独立成线、和当前主问题关系不大的方向

规矩（来自全局偏好）：每条至少写 **一句话 idea、触发它的观察（含数字或现象）、
为什么值得做、最小验证方案、记录日期**。宁可多记一条，也不要因为「可能不成立」而不记。

和白盒主线**直接相关**的 idea 留在 [`HANDOFF-whitebox.md`](HANDOFF-whitebox.md) §17；
这里只放跑题够远、够独立成一条线的。

---

## I1. 多选题形式在 skill / 工具增强类 benchmark 上系统性地虚报效应量（2026-09-11）

**一句话。** 现有 skill benchmark 几乎不控制作答形式，而作答形式本身就决定了
「检索成功」算不算「答对」；多选题把检索、执行、排除干扰项三件事合并成一个准确率。

**触发它的观察。** 同一份 skill、同一批 358 道题、同一个模型，只换作答形式
（`whitebox/tasks/tier_a`，Qwen3-1.7B / 8B）：

| 模型 | 形式 | 无文档 | filler | gold skill | 效应 |
|---|---|---|---|---|---|
| Qwen3-1.7B | 多选 | 0.180 | 0.145 | 0.300 | +12.0pp |
| Qwen3-1.7B | 自由作答 | **0.000** | — | **0.000** | **0** |
| Qwen3-8B | 自由作答 | **0.000** | **0.000** | **0.467** | **+46.7pp** |

1.7B 在自由作答下拿到 skill 会输出**换算表里的因子**（`21`/`180`/`60`），
无文档时输出**题干里的那个数**（`2`）—— 查到了表，不会做那一步乘法。

**为什么值得做（独立成线的理由）。**
1. 可检验的分解：`Δacc(多选) = Δ(检索) + Δ(执行) + Δ(排除干扰项)`，自由作答只测前两项。
   现有 benchmark 报的是哪一个，没人分过。
2. 预测一个**跨规模的交叉**：小模型上多选效应远大于自由作答，大模型上收敛。
   上表已经是这个交叉的两个端点。
3. 它还解释了机制侧的一个大问题（见 I2）：多选把答案压成一个 token。

**最小验证方案。** 3–4 个公开 skill benchmark，**双向**改写（原生多选的改自由作答、
原生自由作答的改多选），2 个模型规模 × 有/无 skill 四臂。
主读数 `Δacc(多选) − Δacc(自由作答)` 随规模的变化；
诊断读数：自由作答的错误答案里有多少**恰好是 skill 文档里出现过的数**
（= 检索成功、执行失败），`whitebox/errors.py` 有分类器雏形。

**killer / 失败模式。** Killer：某个被广泛引用的 skill 效应量在自由作答下缩水一半以上。
失败模式：大部分 benchmark 本来就是自由作答，我们改多选反而在造 artefact ——
所以必须双向做、双向报。

---

## I2. 「单位置 activation patching」的结论有多少是答案长度的产物（2026-09-11）

**一句话。** 单位置补丁能搬运的信息量上限，和**答案占几个 token** 是一回事；
多选题任务的答案恰好是 1 个 token，于是这条方法在多选上系统性地"成功"。

**触发它的观察。** 同一批题、最后一个 prompt 位置、同样的干预：

| 形式 | 模型 | 整体替换成 `h_skill` 修回 R 格的比例 |
|---|---|---|
| 多选 | Qwen3-1.7B | **1.000**（层 24–27） |
| 自由作答 | Qwen3-8B | **0.000**（层 ≤22），最深处 **0.214** |

而这 120 道题的 gold 答案 token 数是 2/3/4，**单 token 的占 0.0%**。

**为什么值得做（独立成线的理由）。** 这是一条**方法学**结论，波及范围远大于 skill：
task vector / function vector / steering 的大量证据来自**单 token 输出的任务**
（分类、A/B/C/D、下一个词）。如果结论对答案长度敏感，那么"一个向量编码了这个任务"
这句话可能主要成立于"任务的输出是一个 token"。

**最小验证方案。** 取一个公开的 function vector 复现，把同一批任务改造成
**k-token 输出**（例如把标签换成两词短语、把数字换成需要两位输出的范围），
测 `recovery(k)`。再做一条**位置容量曲线**：补 1 / 4 / 16 个位置。
预测：recovery 随 k 单调下降，随补丁位置数单调上升，两条曲线大致互补。

**killer / 失败模式。** Killer：`recovery(k=1) ≈ 1.0`、`recovery(k=3) < 0.3`，
在一个已发表的 function vector 设定上复现出来。
失败模式：多 token 任务本来就更难，效应量下降是任务难度而不是方法限制 ——
必须用「同一批题、只换输出格式」来切开，就像上表那样。

---

## I3. 「读文档」这件事本身有一个通用位移，而内容的一部分工作是把它推回去（2026-09-11）

**一句话。** 内容成分 `d = h_skill − h_filler` 与在场成分 `g = h_filler − h_no`
在**每一层**都是负相关，这不是残差该有的样子。

**触发它的观察。** `whitebox/e14_decomp.py`，Tier A，n=200，28 层：
`cos(d, g)` 落在 **−0.32 ~ −0.60**，**每一层都是**；
`PR(d, 去均值)` 只有 **3–8**（d 是 2048 维）；每题 87–99.7% 的 d 在共享均值方向上。
配套的行为读数：单独注入 `g`，把准确率从无文档的 0.231 压到 **0.154–0.179**，
**恰好等于真的把错文档放进 prompt 的 0.179**。

**为什么值得做。** 如果 `g` 是一个可辨认的「我正在读一份文档」状态，
那么它应当 (a) 在 logit lens 下把概率质量推到可识别的一类 token 上，
(b) 随 filler 的"像不像文档"变化，(c) 跨模型守恒。三条都可测。
成立的话，这给出一个与 RAG 的「无关文档伤害性能」现象直接对接的机制解释。

**最小验证方案。** (a) 换四种 filler（随机 token / 打乱的真 skill / 另一领域真文档 /
数字被改掉的同一份 skill）测 `cos(d,g)` 与 `‖g‖`；本仓库已经有后两种
（`tasks/tier_a/ctrl_shuffled`、`ctrl_corrupted`）。
(b) 对 `g` 做 logit lens。(c) 1.7B / 8B 两点先看方向。

**⚠ 失败模式已经部分兑现，必须先读这一段。** `cos(d,g)` **不是独立读数**：
由 `t = g + d` 有余弦定理 `cos(d,g) = (‖t‖² − ‖g‖² − ‖d‖²) / (2‖g‖‖d‖)`，
用实测的三个模长代进去，28 层全部与实测余弦吻合到 **0.002 以内**（已验算）。
所以"内容逆着在场"这句话**等价于** `‖t‖² < ‖g‖² + ‖d‖²`，即两个成分部分抵消。

**真正非平凡的那一半留下来了**：**层 0–16 上 `‖g‖ > ‖t‖`** ——
**一份无关文档把最后一个位置推得比真 skill 还远**（如 L10：39.0 vs 36.0）。
层 17 之后反过来（L21：356 vs 443）。而层 17 恰好是**单独注入 `d` 第一次修好题**的层。
这才是值得追的观察，`cos(d,g)` 只是它的一个换算。

**Killer / 剩下的失败模式。** Killer：`g` 的 logit lens 稳定指向「元话语 / 引用 /
我看到了一份文档」一类 token，且跨模型跨 filler 一致，
并且 `‖g‖ > ‖t‖` 的层区间随 filler 与题的相似度系统变化。
失败模式：`‖g‖ > ‖t‖` 只是"无关文档更难压缩"的一般性质，与 skill 无关 ——
用非 skill 的对照任务（同一个模型、同样长度的两份文档、题与两者都无关）就能排除。

## 一个错误文档要多「像」才会造成伤害？（2026-09-11，⚠ 值得单独立项）

**触发观察（含一次已收回的过度解读，保留以记教训）。**
把接收器 filler 换成与临床无关的中性文本后，n=11 时接收器基线看起来从 0.175 跳到 0.80–1.00，
逐条 dump prompt 也支持这个故事：旧 filler 里是**另一个 QTc 计算器的文档**（Hodges 公式），
模型照着它算所以答错；新 filler 是「档案排版规范」，模型无视它、用对了 Bazett。
**但 n=36 的完整扫描把这个数字拉回 0.278**（无文档 0.028 / 非临床文本 0.278 /
固定错误临床技能 0.167 / 配对中性 0.194 / gold 0.944）。
方向仍然是对的（临床干扰物比无关文本更伤），但 0.11 的差距在 ±0.14 的区间里。
**教训：n=11 的滚动均值不能用来下任何结论，哪怕它有一个说得通的机制故事在旁边支持。**

**科研问题。** 一份错误的 in-context 文档造成的伤害，是它「占了位置」造成的，
还是它「看起来像是对的」造成的？如果是后者，那么 wrong-document 对照的强度
是一个**连续可调的变量**，而现在整个 in-context / skill 文献都把它当成一个二值条件。

**为什么现有工作没覆盖。** 这条线上的论文（skill injection、RAG 噪声鲁棒性、
distractor 检索）几乎都只比较「有/无」或「相关/不相关」两档，没有人把「相关度」
做成一个有序的阶梯并测量伤害随它的变化。而我们手上正好有 55 个同域、同格式、
细粒度相邻（QTc Bazett / Hodges / Fridericia / Framingham）的技能文档，
这是一个现成的、天然有序的干扰物阶梯。

**最小验证实验。** 固定题目集（R 格 40 题），保持文档槽的长度与位置不变，
只换里面的内容，五档：
  (1) 无文档；(2) 非临床中性文本；(3) 不同科室的临床技能；
  (4) 同一量的不同公式（QTc Hodges vs Bazett）；(5) gold。
量 accuracy 与 lp(gold answer)。已经实现为 `wb_spanvec --baseline-sweep`（前四档中的三档）。

**可能的 killer result。** 伤害随相关度**单调上升**，且第 (4) 档比第 (1) 档（无文档）
还差——也就是说，检索到一份「差一点点」的文档比什么都没检索到更糟。
这对 RAG 的实践含义是直接的。目前 n=36 的四档已经排出了正确方向但不显著，
要做就要把 n 提到 200+ 并且把相关度做成真正有序的五档（同一个量的不同公式是最强的一档）。

**失败模式。** 伤害只由长度决定（三档持平），那 (2)(3)(4) 会同高，
这条线就退化成一句「文档内容不重要」，与我们主论文的结论矛盾，也就不值得做了。
目前 n=11 的观察（0.175 vs 0.85）强烈指向前者。

**对主论文的直接影响。** 接收器基线的语义必须写清楚：
它是「一份**貌似相关**的错误文档」，不是「一份中性填充」。
因果实验用 `--filler fixedskill`（一份固定的、题目无关的临床技能文档），
兼顾「与题目无关」和「是活的干扰物」两个要求。

---

## ⚠ 值得单独立项：容量拥挤 —— 一个模型能同时装下几份 skill 文档，可由 PR(d)/d 预测

**记录日期**：2026-09-12

**触发它的观察**：Qwen3-8B（d=4096）的 L8 上，一份 medcalc skill 文档在自己 span 上的
中心化参与比 **PR(d) ≈ 64**，秩截断的膝盖 **k\* ≈ 48**。也就是说，
**一份**文档的作用占掉了约 4096 维里的 48–64 个方向（≈1.2–1.6%）。
如果这个方向数是文档的性质而不是模型的性质（见 `PREREG-ladder.md` 的 H1），
那么在 Qwen3-0.6B（d=1024）上同一份文档要占掉 **4.7%** 的宽度。

**科研问题**：**在一个上下文里放 N 份 skill 文档，模型对每一份的执行能力如何随 N 退化？
退化的拐点是否由 PR(d)/d 预测？** 即：是否存在一个「skill 容量」
`N_max ≈ c · d / PR(d)`，使得超过它之后加文档反而互相抹掉？

**为什么现有工作没覆盖**：
- 长上下文文献量的是**检索**（needle-in-haystack）—— 问的是「能不能找回这段文字」，
  不是「能不能执行这段文字描述的过程」。我们量的是后者（行为准确率）。
- prompt compression（Ardoin et al. 2607.08399）附录里有「一个向量编码 9 个任务」，
  但那 9 个任务是**模型已经会的**、答案单 token 的，属于「选择器」而非「内容」。
  没人在**需要真执行的多步文档**上量过 N 的退化曲线。
- multi-LoRA / skill composition 的文献改参数，不改激活，且几乎不报告几何量。

**最小验证实验**（一天以内，现成代码基本够用）：
1. 取 K 个 calculator 的 skill 文档，构造上下文里放 N ∈ {1,2,4,8,16} 份（其中一份是 gold）。
2. 量 gold 那一题的准确率 vs N，以及**干扰文档的位置效应**（gold 在前/在后）。
3. 同时量每份文档 span 的 PR(d) 和它们之间的**子空间重叠**（principal angles）。
4. 跨模型阶梯（0.6B / 1.7B / 8B / 14B）重复，检验拐点是否 ∝ d/PR(d)。

**killer result**：退化拐点在四个模型上按 d/PR(d) 对齐成一条曲线 ——
那就把「上下文里能塞几个技能」从工程经验变成一个可计算的量。

**失败模式**：(a) 退化主要由**注意力稀释**而非维度拥挤造成，那 PR 预测不出拐点，
但我们能用子空间重叠把两者分开（重叠高的文档对应当退化更快；若与重叠无关则是注意力）；
(b) 模型本来就会做其中一些 calculator，污染准确率 —— 用我们已有的「限制集」办法解决。

**与主线的关系**：主线是「一份文档的作用住在哪、有多大」；这条是它的直接推论
「所以能同时住几份」。可以作为本文的一个 discussion 小节 + 下一篇的主线。

---

## ⚠ 值得单独立项：思维链把 activation patching 的噪声地板抬高一个量级

**记录日期**：2026-09-12

**触发它的观察**：我们的 `self` 臂把接收器自己的 span 状态**原样写回**接收器，
是逐比特的恒等操作，行为上必须与不打补丁完全一致。
在 93 个结果文件、**1978 条成对观测**上实测：

```
self == receiver : 1898/1978 = 0.9596      → 4.0% 的题在恒等补丁下翻面
翻转方向 25 上 / 37 下，符号检验 p = 0.162 → 对称，是数值噪声不是 bug
cell F（持续失败）子集上高达 8/40 = 20%
```

**科研问题**：**当被测行为是一条长思维链而不是单个 token 时，
activation patching 的可复现噪声地板是多少，它随什么缩放（链长、bf16 vs fp32、
温度、题目难度、离决策边界的距离），以及现有文献里有多少「小效应」实际上落在这个地板以下？**

**为什么现有工作没覆盖**：
- 经典 patching 文献（IOI、事实召回、function vector）测的是**单个 token 的 logit 或 argmax**，
  一个 token 的 argmax 对 1e-3 的扰动稳定得多，所以这个地板小到看不见。
- 推理可解释性转向 CoT 之后，大家把同一套 patching 直接搬过来，
  但**没有人报告恒等补丁的翻转率**。我们没有搜到任何一篇报告这个对照的。
- 相关但不同：CoT 忠实性（faithfulness）文献问的是「解释和答案一致吗」，
  不是「同一次计算重跑一遍答案一样吗」。

**最小验证实验**（现成代码，一天）：
1. 只跑 `self` 臂，扫 (a) 生成长度 max_new ∈ {50,100,200,400,900}；
   (b) dtype ∈ {bf16, fp16, fp32}；(c) attn ∈ {sdpa, eager}；(d) 有/无 CoT 提示。
   每格 n≥100，报告翻转率。
2. 关键预测：**翻转率随生成长度单调上升**，fp32 下大幅下降（若是纯数值）；
   若 fp32 下仍然有，那就不是数值而是采样/kv-cache 的问题，更值得追。
3. 同时量「翻转的题」是否就是「多次重跑本来就不稳的题」——
   用同一条件重跑 5 次算一个 per-item 稳定度，看两者相关性。

**killer result**：给出一条 `翻转率 ≈ f(链长, 精度)` 的曲线，
并指出**低于这条曲线的效应在 CoT 上不可报告**。这直接给整个 CoT 可解释性方向一把尺子。
顺带可以回头检查几篇已发表工作报告的小效应是否在地板以下。

**失败模式**：(a) 大家其实都知道，只是没写——需要更系统的检索（搜
"identity patch" / "null intervention" / "reproducibility of activation patching"）；
(b) 翻转率强依赖于我们的 grader，若 grader 本身有随机性就是伪影
（我们的 grader 是确定性的数值匹配，已排除）。

**与主线的关系**：主线**必须**报告这个地板（论文方法节），否则任何 <4pp 的读数都不可信。
但完整的标度研究够独立成一篇方法学短文。

---

## I4. 移植失败的深度窗口是「读者侧」的，不是「内容侧」的——而且能不解码就测出来（2026-09-16）

**一句话。** 把文档内容写进深层（L≥16）之所以失效，不是因为深层表示装不下内容
（rank 塌缩那套解释），而是因为消费位置（问题文本）在 L≤16 就已经把 filler 读完了；
内容在 L0+1 之后**逐比特等于 gold**，却没人再去读它。

**触发它的观察（2026-09-16，Qwen3-8B，24 题 / 6 calculator，`heads-smoke2`）。**
新测量 `closed@last` = 注入在 L0 时、消费位置（span 之后 64 个位置）的
「receiver→gold 缺口」在最后一层被闭合的比例，**完全不解码**：

| 注入层 | L0 | L4 | L8 | L12 | **L16** | L20 | L24 |
|---|---|---|---|---|---|---|---|
| `closed@last` | 0.85 | 0.60 | 0.52 | 0.35 | **0.10** | 0.09 | 0.07 |
| 行为学 ρ（正文 §5.1） | 1.00 | 1.06 | 1.03 | 0.81 | **0.14** | — | 0.00 |

断崖位置一致（L12→L16）。同时，**下游读出恢复率 `recovery(L16)` 仍有 0.56–0.99**——
即注入之后，深层注意力头照样把 span 的内容读走了，只是没用。

**为什么这是「读者侧」的证明（不是又一个相关性）。**
注入 `h_gold` 到 L16 之后，span 位置在 L17+ 只 attend 到（a）两个 run 完全相同的前缀
和（b）同样被改写成 gold 的更早 span 位置。所以 **span 自己的状态从 L17 起和 gold 逐比特相同**。
内容既在、也被读出，行为却塌了 → 丢的只能是 L≤16 那段里消费位置对 span 的读。
这条逻辑**直接反驳 rank 塌缩解释**：L17+ 的内容就是 gold 的内容，谈不上「深层装不下」。

**为什么值得独立立项。**
1. 给出一个**不解码的代理指标**预测 activation patching 的有效层窗口。patching 文献里
   层窗口一律靠跑完整实验扫出来；如果这个代理在多个模型/任务上成立，扫描成本降一个量级。
2. 把「信息在不在」和「有没有被读」分开，是 patching 方法学里一直混着的两件事。
3. 可证伪且已有失败模式：若在别的模型上 `closed` 曲线断崖与 ρ 断崖错开 ≥4 层，代理就不成立。

**最小验证方案。**
- 已有：Qwen3-8B 上 24 题的曲线（上表）。下一步全量 469 题 + 全层扫描（`full-heads`，约 1–2 h）。
- 跨模型：Mistral-7B（必须 L4，见接管文档 §7）、Qwen3-0.6B（28 层，已有行为学深度曲线）。
- 因果确认：按 `‖dg‖/‖resid‖` 选 top-k 读者头做定向敲除，对照同层随机 k 头（`wb_heads --mode knockout`）。

**可能的 killer result。** `closed@last` 与 ρ 在 3 个模型上的断崖层都对得上（±2 层），
且 `recovery` 在所有注入层都保持高位——一张图讲完「内容在、没人读」。
**失败模式。** `closed` 在 L4/L8 只有 0.5–0.6 而 ρ 接近 1.0，说明行为是饱和的；
若换个任务后行为不饱和，两条曲线的形状可能对不上，那就只能声称「断崖位置」而不是「曲线」。

**【2026-09-16 当日修正，必须连同上文一起读】**

跑完归一化 P1 和全层扫描（`heads-smoke2`，24 题 × 7 个注入层）之后，上面的强版本**站不住**：

1. **读出确实有层带，但它解释不了断崖。** 按残差范数归一化后，读出集中在 **L12–L21（峰值 L18）**，
   原始 `‖dg‖` 的深层主导是范数假象。但由此得到的累计读出份额
   `C(8)=0.19 / C(12)=0.33 / C(16)=0.50` 是**平滑下降**的，而行为 ρ 是
   `1.03 / 0.81 / 0.14` 的**断崖**。「注入越晚、已读走的越多」只能预测斜坡，预测不出崖。
2. **头不是小电路。** top 1% 的头（11/1152）只承担 11.6% 的读出，top 5% 承担 32.9%。
   原计划的「敲掉 top-k 头」那条 P3，前提就不成立。
3. **`closed@last` 与 ρ 的一致是真的但半同义**：r=0.916，最大跌幅两边都落在 **12→16**。
   可它测的是「末层状态离 gold 多远」，和 ρ 测的是同一个东西的两种读数。

**于是真正没人解释的问题换成了这个** ⚠：

> **交接为什么是断崖而不是斜坡？** 读出份额、消费位置缺口（L16 处 0.33、L20 峰值 0.38）
> 都是连续变化的，行为却在 L12→L16 之间从 0.81 掉到 0.14。中间存在一个**阈值式非线性**，
> 现有的头级读出账本预测不出来。

这比原来的 idea 更值得做，因为它是**现有工作（Retrieval Heads ICLR 2025、ReDeEP ICLR 2025、
Geva EMNLP 2023）都不覆盖**的：那些工作说的是「哪些头读上下文」，没有一篇解释
「为什么 patching 的有效层窗口是一个突变边界」。最小验证：在 L12–L16 之间以 1 层为步长扫
（L13/14/15），看行为是不是真的突变；同时看消费位置缺口有没有对应的相变量
（例如 gold 与 receiver 在某个低维子空间上的可分性在该层突然丢失）。

**同时记下一条反事实**：消费位置的 receiver→gold 缺口在 **L20 达到峰值 0.38 后反而收窄到 L34 的 0.20**，
即网络后段在部分「自愈」。这与「差距只会累积」的直觉相反，值得单独看。

---

## I6. 注入的 skill 状态必须「逐位」放回原位——平移 1 个 token 就失效（2026-09-19）

**一句话。** 把整段 skill span 的有技能状态整体平移 1 个位置再写回（第 8 层，Qwen3-8B，MedCalc），恢复率从 0.89 掉到 0.15–0.22；
而原位写入、只去掉首或尾 1–16 个位置，恢复率不变（0.89–0.92）。

**触发它的观察（135 题 / 14 calculator，按 calculator 聚类 95% CI）。**
循环平移 D=1/4/16/64/256：.18/.06/.03/.07/.06；不卷绕前移 1/4/16：.15/.05/.06；不卷绕后移 1/4/16：.22/.12/.07；
原位但去掉前 1/16 位：.89/.90，去掉后 1/16 位：.89/.92；整段原位 .89。
同一实验里还有：同预算下连续块远好于分散位置（256 位：1 块 .37 → 32 块 .08），半段连续块若以流程段为中心 .82。

**为什么值得做。** 平移 1 位保留了 span 内部所有相对位置与相邻关系，只改变了 span 与「读它的位置」之间的相对距离（+1）
以及每个状态的绝对位置。按常见理解（基于内容的注意力读取），这点偏移不该致命。它说明下游读取 skill 时
依赖的是「位置与内容的精确绑定」，而不仅是内容——这与 function/task vector 文献中「一个向量携带任务」的图景正相反，
也不同于 KV 压缩文献关心的「保留哪些 token」。现有工作（Label-words anchors；Probing the Prompt KV Cache, arXiv 2605.30574）
都没有做过「同内容、错一位」的干预。

**最小验证方案。**
1. 相位补偿：平移 1 位的同时把这些位置的 `position_ids` 也改回原值（RoPE 相位还原）。恢复 → 失效来自 RoPE 相位错配；不恢复 → 来自状态里编码的位置信息与邻居的耦合。
2. 在不同注入层（0/4/8/12）重复平移 1 位：要求「逐位精确」是否随层变化（和 12→13 的交接断崖有无关系）。
3. 换模型（Qwen3-0.6B、Mistral）与换任务（TheoremQA）各跑一组 sh1 / xf16。

**killer / 失败模式。** Killer：相位补偿后完全恢复 → 「skill 读取依赖 RoPE 相对相位的精确对齐」，可以直接连到长上下文位置插值/外推的文献。
失败模式：只在 Qwen3-8B 第 8 层成立，换层或换模型就不成立。
